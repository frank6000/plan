# ออกแบบระบบติดตามโครงการ กิจกรรม งบประมาณ และ KPI

เอกสารนี้ออกแบบจากข้อมูลจริงในไฟล์ Excel ที่แนบมา (`นำเสนอแผนงาน_โครงการ_PM`) ซึ่งมีชีตข้อมูล เช่น
`DATA` (โครงการย่อย), `กิจกรรม`, `tracking`, `budget`, `KPI`, `user`, `สถานะโครงการ` ฯลฯ
ยังไม่มีการเขียนโค้ด — เป็นแบบร่างสถาปัตยกรรมและโครงสร้างข้อมูลเพื่อให้ตรวจทานก่อน

---

## 1. เป้าหมายของระบบ

| ด้าน | รายละเอียด |
|---|---|
| ติดตามโครงการ | บันทึกโครงการ, ผ่านขั้นตอนอนุมัติ 15 สถานะ (เสนอ → หัวหน้างานตรวจ → สสอ. → รพ. → สสจ. → อนุมัติ → **ขอโอนเงิน** → **ดำเนินการเรียบร้อย**) — เฉพาะ "ส่งขอจัดกิจกรรม" ย้ายไปผูกกับกิจกรรมแต่ละรายการแทน ส่วน "ขอโอนเงิน" เป็นรายโครงการเหมือนเดิม (ดูข้อ 5), ดูประวัติการเปลี่ยนสถานะ, **หน่วยบริการเห็นเฉพาะโครงการของหน่วยตนเอง** |
| ติดตามกิจกรรม | บันทึกกิจกรรมย่อยของแต่ละโครงการ พร้อมกลุ่มเป้าหมาย งบที่ตั้งไว้ และเปอร์เซ็นต์ความคืบหน้า |
| ติดตามงบประมาณ | เทียบงบอนุมัติ vs เบิกจ่ายจริง ทั้งระดับโครงการ/กิจกรรม/กลุ่มงาน พร้อมกราฟและ % การเบิกจ่าย |
| เชื่อมโยง KPI | ผูกโครงการกับตัวชี้วัดระดับประเทศ/ระดับโครงการ บันทึกค่าเป้าหมาย และติดตามผลจริงเป็นช่วงเวลา (time-series) |
| อนุมัติหลายระดับ | ปุ่ม **"อนุมัติ" / "ตีกลับ"** ตามลำดับขั้นสำหรับงานประจำวัน + ผู้มีสิทธิ์สูง (สสจ./admin) **เลือกสถานะได้อิสระ** เพื่อกรณีพิเศษ |
| แจ้งเตือนอีเมล | ส่งอีเมลอัตโนมัติเมื่อสถานะโครงการเปลี่ยน และแจ้งเตือนล่วงหน้าเมื่อใกล้ครบกำหนดดำเนินการ |
| ประเภทโครงการ | แยก **โครงการทั่วไป** กับ **โครงการจัดซื้อวัสดุ** ตั้งแต่ตอนสร้างโครงการ (ตัดโมดูลคำขอวัสดุอิสระออก) |

---

## 2. สถาปัตยกรรมระบบ (Architecture)

```
Browser (Bootstrap 5 UI)
      │  HTTPS
      ▼
PHP 8.x (โครงสร้างแบบ MVC-lite, ไม่ใช้ framework หนัก)
  ├─ /config      → การเชื่อมต่อฐานข้อมูล (PDO)
  ├─ /includes    → auth, helper functions, header/footer
  ├─ /modules     → projects, activities, budget, tracking, kpi, users
  ├─ /api         → endpoint คืนค่า JSON ให้กราฟ (Chart.js)
  └─ /assets      → CSS/JS
      │  PDO (prepared statements)
      ▼
MySQL 8 (InnoDB, utf8mb4)
```

**แนวทางหลัก**
- ใช้ PDO + prepared statements ทุกจุดเพื่อป้องกัน SQL Injection
- **Authentication มี 3 ช่องทาง: Google OAuth 2.0, LINE Login, และ Username/Password** (ช่องทางหลังสุดสมัครได้เอง ผ่านการยืนยันอีเมลก่อน — ดูข้อ 12.4) — หลังยืนยันตัวตนสำเร็จ ระบบสร้าง session ปกติ, สิทธิ์ผู้ใช้แยกตาม `level` (หน่วยบริการ / สสอ. / รพ. / สสจ. / admin) เหมือนเดิม
- ทุกโมดูลอ่าน/เขียนผ่านตารางกลาง (lookup tables) เพื่อให้ dropdown และรายงานสอดคล้องกันเสมอ
- แยก "ค่าปัจจุบัน" ออกจาก "ประวัติ" เช่น `projects.status_id` (สถานะล่าสุด) กับ `project_tracking_log` (ประวัติทุกครั้ง)

---

## 3. แผนภาพความสัมพันธ์ข้อมูล (ER Overview)

```
-- ลำดับชั้นสำหรับ dropdown แบบ cascading (เลือกแผน → เห็นเฉพาะโครงการหลักที่เกี่ยวข้อง → เห็นเฉพาะ KPI ที่เกี่ยวข้อง)
plans (13) ──► main_projects (29, มี plan_id FK) ──► kpi_master (38, มี main_project_id FK)
   1            │  N                                    │  N
                └── main_projects.plan_id ────┘         └── kpi_master.main_project_id
(ที่มา: จับคู่จากชีต "Copy of KPI" ตอนนำเข้าข้อมูล แล้วให้ admin ตรวจสอบ/แก้ไขผ่านหน้าจัดการ — ดูข้อ 10.1)

departments ─┐
plans ───────┤
main_projects┤
strategic_issues├──► projects (โครงการย่อย) ──► **fiscal_year_id** (FK fiscal_years, ปีงบประมาณ พ.ศ. 4 หลัก)
moph_excellence│    project_type: 'ทั่วไป' | 'จัดซื้อวัสดุ'
health_networks─┤   owner_office_id ──► departments (สสอ.-level) **หรือ** owner_facility_id ──► health_facilities (ข้อ 23, ระดับหน่วยบริการ)
                     budget_approved = SUM(activities.budget_plan) ← **คำนวณอัตโนมัติ ไม่กรอกตรง**
                     **situation_analysis, situation_data_source** (ข้อ 22.3) / **parent_project_id** (FK ตัวเอง, ความต่อเนื่องข้ามปี ข้อ 22.5)
                     ──► project_priority_scores (1:1, คะแนน BPR ข้อ 22.4)
project_status (15 ขั้น — จบที่ "ดำเนินการเรียบร้อย" รวมขั้น "ขอโอนเงิน" รายโครงการ) ──┐   │
                          │   ├──► project_tracking_log (ประวัติสถานะระดับโครงการ, 1:N)
                          │   │        └──► (trigger) notification_log ──► ส่งอีเมลเมื่อสถานะเปลี่ยน
                          │   │
                          │   ├──► activities (1:N — **จุดที่เก็บงบประมาณ, แหล่งงบ, และช่วงเวลาดำเนินการจริง**)
                          │   │        ├─ budget_plan, budget_source_id (FK budget_sources) ← ย้ายมาจากระดับโครงการ
                          │   │        ├─ objective, kpi_target_text, target_area, target_group, responsible_person
                          │   │        ├─ **prevention_level** (ปฐมภูมิ/ทุติยภูมิ/ตติยภูมิ — ข้อ 22.2)
                          │   │        ├─ status_id (FK **activity_status** — สถานะการขอจัดกิจกรรม/เสร็จสิ้นเฉพาะกิจกรรมนั้น แยกจากสถานะโครงการ)
                          │   │        ├──► activity_tracking_log (ประวัติสถานะระดับกิจกรรม, 1:N — คู่ขนานกับ project_tracking_log)
                          │   │        ├──► activity_months (M:N เดือนที่ทำกิจกรรม — แทนที่ project_months เดิม)
                          │   │        ├──► activity_logs (% ความคืบหน้า, 1:N — คนละเรื่องกับ activity_tracking_log)
                          │   │        └──► budget_disbursements (การเบิกจ่ายจริง ผูกกับกิจกรรมเสมอ, 1:N)
                          │   │
                          │   ├──► project_materials (1:N, ใช้เมื่อ project_type = 'จัดซื้อวัสดุ')
                          │   │        └──► material_catalog (lookup ชื่อวัสดุ, ไม่บังคับ)
                          │   │
                          │   └──► project_kpi (M:N กับ kpi_master) ──► kpi_progress (ผลจริงตามช่วงเวลา, 1:N)
                          │
activity_status (3 ขั้น — รอดำเนินการ → ส่งขอจัดกิจกรรม → **ดำเนินการเรียบร้อย** [สุดท้าย, เฉพาะกิจกรรมนั้น]) ──► activities.status_id
budget_sources ───► activities.budget_source_id (มี category: NON_UC / เงินบำรุง / อื่นๆ สำหรับ pivot รายงาน)
users ──► office_id (FK departments, ใช้กรองสิทธิ์) / projects.created_by / activities.created_by /
          budget_disbursements.recorded_by / kpi_progress.recorded_by

-- ตารางข้อมูลหลักที่แก้ไขได้ผ่านหน้าจอ (รายละเอียดข้อ 10):
project_templates (แม่แบบโครงการ ใช้เติมฟอร์มสร้างโครงการใหม่อัตโนมัติ)
priority_levels (ตั้งค่าได้เอง) ──► project_priority_levels (M:N กับ projects — ข้อ 19)
health_facilities (ตั้งค่าได้เอง — ข้อ 23) ──► project_facilities / activity_facilities (M:N)
kpi_master (KPI หลัก) ──► objectives (1:N, มีกรอบเวลา/ไตรมาส) ──► key_results (1:N) ──► key_result_progress (1:N)
                                                                              └──► kpi_references (1:N ลิงก์แหล่งอ้างอิง — ข้อ 18.6)
```

### ตารางหลัก (Master / Lookup)
| ตาราง | ที่มาจากชีต | ใช้เป็น dropdown ใน |
|---|---|---|
| `fiscal_years` (ใหม่ — เพิ่มเพื่อความสมบูรณ์) | — (เดิมเป็นแค่ตัวเลขในหัวรายงาน) | id, **year_be** (ปี พ.ศ. 4 หลัก เช่น 2569), label (เช่น "ปีงบประมาณ พ.ศ. 2569"), start_date/end_date (1 ต.ค. – 30 ก.ย. ของปีนั้น), is_active — ทำให้เปิด/ปิดปีงบประมาณได้ และเป็นจุดเดียวที่ควบคุมว่าปีไหน "ใช้งานอยู่" (ค่าเริ่มต้นตอนสร้างโครงการใหม่) แทนการกรอกตัวเลขปีอิสระ |
| `departments` | CodeDepartment | ฟอร์มโครงการ, รายงานงบประมาณ |
| `plans` | planCode | ฟอร์มโครงการ |
| `main_projects` | pjCode | ฟอร์มโครงการ — มี **`plan_id`** (FK `plans`) ทำให้เลือกแผนงานก่อนแล้ว dropdown นี้กรองเฉพาะโครงการหลักในแผนนั้น |
| `strategic_issues` | StrategicIssues | ฟอร์มโครงการ |
| `moph_excellence` | mophEx | ฟอร์มโครงการ |
| `budget_sources` | budget | **ฟอร์มกิจกรรม** (ย้ายจากฟอร์มโครงการ), รายงานงบประมาณ — เพิ่มคอลัมน์ **`category`** (`NON_UC`/`เงินบำรุง`/`อื่นๆ`) สำหรับจัดกลุ่มคอลัมน์ตอน pivot รายงาน และเพิ่ม 2 รายการที่พบใหม่จากตัวอย่างรายงาน: `สป.` (สป.(ผลผลิต), category=NON_UC) และ `กรม` (งบกรม, category=NON_UC) |
| `health_networks` (ใหม่) | — (พบจากหัวรายงานตัวอย่าง "เครือข่ายบริการสุขภาพอำเภอเมืองยโสธร") | ฟอร์มโครงการ/รายงาน — ใช้เป็นหัวรายงานระดับบนสุด กรณีระบบดูแลหลายเครือข่าย/อำเภอในอนาคต |
| `project_status` | สถานะโครงการ — **คง 15 ขั้น** (จากเดิม 17 ขั้น) จบที่ "ดำเนินการเรียบร้อย" (`15d9`) — มีแค่ "ส่งขอจัดกิจกรรม" (`14d8`) เท่านั้นที่ย้ายไปผูกกับกิจกรรมแทน ส่วน "ส่งขอโอนเงิน" (`14d9`) **ยังเป็นรายโครงการเหมือนเดิม** เพราะเงินโอนเข้าหน่วยงานเป็นก้อนต่อโครงการ ไม่ได้แยกโอนทีละกิจกรรม | ฟอร์มโครงการ, การติดตามสถานะโครงการ — **ปรับปรุงใหม่**: เพิ่ม `next_status_id`/`return_status_id` (FK ตัวเอง) |
| `activity_status` (ใหม่) | สถานะการดำเนินกิจกรรม **3 ขั้น**: **รอดำเนินการ → ส่งขอจัดกิจกรรม → ดำเนินการเรียบร้อย** (สถานะสุดท้าย ของกิจกรรมนั้นเอง — `next_status_id` เป็น NULL) | ฟอร์มกิจกรรม, การติดตามสถานะกิจกรรม — โครงสร้างเหมือน `project_status` ทุกอย่าง (code, name, sort_order, next_status_id, return_status_id) แต่แยกตารางเพราะเป็นคนละ workflow กัน (อนุมัติ/ขอโอนเงิน ระดับโครงการ vs จัดกิจกรรมระดับกิจกรรม) |
| `months_ref` | month | เลือกช่วงเวลาดำเนินการ (พร้อมไตรมาส) |
| `kpi_master` | KPI (38 รายการ) | ผูก KPI กับโครงการ — มี **`main_project_id`** (FK `main_projects`, nullable) ทำให้เลือกโครงการหลักก่อนแล้ว dropdown นี้กรองเฉพาะ KPI ของโครงการหลักนั้น; เป็น **KPI หลักเท่านั้น** ไม่มีตัวย่อยในตารางนี้อีกต่อไป — ตัวชี้วัดย่อยที่ผู้ใช้กำหนดเองย้ายไปอยู่ในรูปแบบ OKR (`objectives`/`key_results`) แทน (ข้อ 18); เพิ่ม **`result_level`** (`output`/`outcome`/`impact` — ข้อ 22.1) |
| `material_catalog` | codeMat | เลือกชื่อวัสดุ (autocomplete) ในโครงการประเภท "จัดซื้อวัสดุ" — ไม่ใช่โมดูลอิสระแล้ว |
| `priority_levels` (ใหม่ — ข้อ 19) | — (ใหม่ ตั้งค่าได้เอง) | ระดับความสำคัญ/ที่มาของโครงการ (นโยบายระดับเขต, นโยบายระดับจังหวัด, ประเด็น พชอ., ปัญหาสุขภาพในพื้นที่ ฯลฯ) — เพิ่ม/แก้/ปิดใช้งานได้เองผ่านหน้าจัดการข้อมูลหลัก |
| `health_facilities` (ใหม่ — ข้อ 23) | — (ใหม่ ตั้งค่าได้เอง) | รายชื่อหน่วยบริการจริง (รพ.สต./ศสช./รพ.) แยกจาก `departments` ที่เป็นกลุ่มงานภายใน สสอ. — ใช้ระบุพื้นที่เป้าหมายเชิงโครงสร้างและกรองสิทธิ์การมองเห็นของผู้ใช้ระดับหน่วยบริการ |

### ตารางธุรกรรม (Transaction)
| ตาราง | หน้าที่ | ฟิลด์สำคัญ |
|---|---|---|
| `projects` | โครงการย่อย 1 แถว/โครงการ | project_code (unique), **fiscal_year_id** (FK `fiscal_years` แทนเลขปีลอย ๆ), **network_id** (FK `health_networks`), **project_type** (`general`/`material_procurement`), **owner_office_id** (FK departments, สสอ.-level) **หรือ owner_facility_id** (FK `health_facilities`, ระดับหน่วยบริการ — ข้อ 23.3, ใช้กรองสิทธิ์การมองเห็น), **planned_start_date**, **planned_end_date** (สำหรับแจ้งเตือนใกล้ครบกำหนด), **budget_approved** (คอลัมน์เก็บผลรวม — อัปเดตอัตโนมัติทุกครั้งที่กิจกรรมมีการเพิ่ม/แก้ไข/ลบงบ ไม่ให้ผู้ใช้กรอกตรงในฟอร์มโครงการ), **situation_analysis**, **situation_data_source** (สถานการณ์ปัญหา/แหล่งข้อมูลอ้างอิง ข้อ 22.3), **parent_project_id** (FK ตัวเอง — โยงความต่อเนื่องข้ามปี ข้อ 22.5), status_id, FK ไป master ทั้งหมด |
| `project_tracking_log` | ประวัติการเปลี่ยนสถานะ | project_id, status_id, tracking_date, remark, pm_name, recorded_by |
| `activities` | กิจกรรมของโครงการ — **จุดที่ตั้งงบประมาณ, แหล่งงบ, และรายละเอียดเชิงปฏิบัติการจริง** (ตามแบบฟอร์มแผนปฏิบัติการตัวอย่าง) | project_id, activity_name, **objective** (วัตถุประสงค์เฉพาะกิจกรรม), **kpi_target_text** (ตัวชี้วัดและค่าเป้าหมายของกิจกรรมนี้), **target_area** (พื้นที่เป้าหมาย — แยกจาก target_group), target_group, **prevention_level** (`primary`/`secondary`/`tertiary`, nullable — ระดับการป้องกันโรค ข้อ 22.2), **budget_plan** (`DECIMAL NOT NULL DEFAULT 0` — บังคับมีค่าเสมอ อนุญาตเป็น 0 ได้สำหรับกิจกรรมที่ไม่มีค่าใช้จ่าย ดูข้อ 20), **budget_source_id** (FK `budget_sources` — ย้ายมาจากระดับโครงการ), **responsible_person** (ผู้รับผิดชอบกิจกรรม — อาจต่างจากผู้รับผิดชอบโครงการ), **status_id** (FK `activity_status` — เดิมเป็น ENUM ธรรมดา เปลี่ยนเป็น FK ตาราง status เพื่อรองรับ workflow "ส่งขอจัดกิจกรรม → เสร็จสิ้น" เฉพาะของกิจกรรมนั้น — ไม่รวมขั้นขอโอนเงินซึ่งยังเป็นรายโครงการ) |
| `activity_tracking_log` (ใหม่ — คู่ขนานกับ `project_tracking_log`) | ประวัติการเปลี่ยนสถานะระดับกิจกรรม (ส่งขอจัดกิจกรรม → เสร็จสิ้น) | activity_id, status_id, tracking_date, remark, recorded_by |
| `activity_months` (ใหม่ แทนที่ `project_months`) | เดือนที่ทำกิจกรรม (ตรงกับตาราง "x" รายเดือน ต.ค.–ก.ย. ในรายงานตัวอย่าง) | activity_id, month_id |
| `activity_logs` | ความคืบหน้ากิจกรรมเป็นช่วงเวลา (% ความคืบหน้า + บันทึกอิสระ — คนละเรื่องกับสถานะ workflow ทางการใน `activity_tracking_log`) | activity_id, log_date, progress_percent |
| `budget_disbursements` | รายการเบิกจ่ายจริง — ผูกกับกิจกรรมเสมอ (project_id ดึงต่อผ่าน activity เพื่อ denormalize ให้ query ไว) | activity_id (NOT NULL), project_id, amount, disbursement_date |
| `project_kpi` | ผูก KPI + ค่าเป้าหมายต่อโครงการ (แบบต่อเนื่อง ไม่มีกรอบเวลา) | project_id, kpi_id, target_value, target_unit |
| `kpi_progress` | ผลจริงของ KPI ตามช่วงเวลา (แบบต่อเนื่อง) | project_kpi_id, record_date, actual_value |
| `objectives` (ใหม่ — ข้อ 18, แนวคิด OKR) | Objective ของโครงการ มีกรอบเวลา (ไตรมาส/ปีงบประมาณ) ผูกกับ KPI หลักเสมอ | project_id, kpi_id (FK kpi_master), title, fiscal_year_id, quarter, status, final_score |
| `key_results` (ใหม่ — ข้อ 18) | Key Result ภายใต้ Objective พร้อมทิศทาง (เพิ่ม/ลด) และคะแนนตอนปิดรอบ | objective_id, description, baseline_value, target_value, target_direction, unit, **result_level** (`output`/`outcome`/`impact` — ข้อ 22.1), final_score |
| `key_result_progress` (ใหม่ — ข้อ 18) | ค่าจริงของ Key Result ตามช่วงเวลาระหว่างรอบ | key_result_id, record_date, actual_value, note, recorded_by |
| `project_materials` | รายการวัสดุ ใช้เฉพาะโครงการประเภท "จัดซื้อวัสดุ" | project_id, item_name, quantity, unit_price, total_price (คำนวณอัตโนมัติ) |
| `project_priority_levels` (ใหม่ — ข้อ 19) | ผูกโครงการกับระดับความสำคัญ แบบ M:N (1 โครงการเข้าข่ายได้หลายระดับ) | project_id, priority_level_id, note |
| `project_facilities` (ใหม่ — ข้อ 23.2) | หน่วยบริการที่เข้าร่วม/เป็นเป้าหมายของโครงการ แบบ M:N | project_id, facility_id |
| `activity_facilities` (ใหม่ — ข้อ 23.2) | หน่วยบริการที่เกี่ยวข้องเฉพาะกิจกรรม แบบ M:N (ละเอียดกว่าระดับโครงการ) | activity_id, facility_id |
| `project_priority_scores` (ใหม่ — ข้อ 22.4) | คะแนนจัดลำดับความสำคัญเชิงปริมาณตามสูตร Hanlon's BPR | project_id, problem_size_score, problem_severity_score, intervention_effectiveness_score, pearl_propriety/economics/acceptability/resources/legality, computed_bpr_score, scored_by, scored_at |
| `kpi_references` (ใหม่ — ข้อ 18.6) | ลิงก์แหล่งอ้างอิงของ Key Result แบบ 1:N | key_result_id, url, label, added_by, added_at |
| `notification_log` | ประวัติอีเมลที่ส่งแล้ว (กันส่งซ้ำ) | project_id, type (`status_change`/`deadline_reminder`), sent_to, sent_at |
| `password_resets` (ใหม่ — ข้อ 12.5) | โทเคนสำหรับฟีเจอร์ลืมรหัสผ่าน (เฉพาะบัญชี `local`) | user_id, token, expires_at |
| `email_verifications` (ใหม่ — ข้อ 12.4) | โทเคนยืนยันอีเมลตอนสมัครสมาชิกด้วยตนเอง (เฉพาะบัญชี `local` ที่สมัครเอง) | user_id, token, expires_at |
| `attachments` (ใหม่ — เพิ่มเพื่อความสมบูรณ์) | ไฟล์แนบ ใช้ร่วมกันได้หลายจุด (เอกสารโครงการ, หลักฐานเบิกจ่าย, รูปกิจกรรม) แบบตารางเดียว (polymorphic) | related_table (`projects`/`activities`/`budget_disbursements`), related_id, file_name, file_path, uploaded_by, uploaded_at |
| `audit_log` (ใหม่ — ดูรายละเอียดเต็มในข้อ 15) | หัวรายการการแก้ไข 1 แถว/1 ครั้งที่บันทึก (ใคร แก้ตารางไหน แถวไหน เมื่อไร) | table_name, record_id, action (`create`/`update`/`delete`), changed_by, changed_at, ip_address |
| `audit_log_details` (ใหม่ — ดูข้อ 15) | รายละเอียดระดับ "ฟิลด์" ของการแก้ไขแต่ละครั้ง (แก้ "อะไร" จากค่าไหนเป็นค่าไหน) | audit_log_id (FK), field_name, old_value, new_value |
| `users` | ผู้ใช้งาน/สิทธิ์ — **login ได้ 3 ช่องทาง: Google, LINE, Username/Password** | **auth_provider** (`google`/`line`/`local`), **provider_user_id** (เฉพาะ google/line), **username**, **password_hash** (เฉพาะ local), **email_verified_at** (เฉพาะ local — ยืนยันอีเมลก่อนถึงจะรออนุมัติจาก admin ได้ ดูข้อ 12.4), **must_change_password**, **failed_login_count**, **locked_until**, email, name, avatar_url, level, status(Approve/Pending), **office_id** (FK departments, ผู้ใช้ระดับ สสอ./รพ./สสจ.) **หรือ facility_id** (FK `health_facilities`, ผู้ใช้ระดับหน่วยบริการ — ข้อ 23.3) |

**หลักการออกแบบสำคัญ (แก้ไขล่าสุด):**
- **งบประมาณตั้งต้นอยู่ที่ระดับ "กิจกรรม" ไม่ใช่ระดับโครงการ** — ฟอร์มเพิ่ม/แก้ไขโครงการจะไม่มีช่องกรอกงบประมาณอีกต่อไป ผู้ใช้ตั้งงบทีละกิจกรรม (`activities.budget_plan`) แล้วระบบรวมเป็นงบของโครงการโดยอัตโนมัติ (`projects.budget_approved = SUM(activities.budget_plan)`) — อัปเดตทุกครั้งที่เพิ่ม/แก้ไข/ลบกิจกรรม (ผ่าน trigger หรือคำนวณสดตอน query ก็ได้ แล้วแต่ที่จะเลือกตอน implement)
- **การเบิกจ่ายจริงผูกกับกิจกรรมเสมอ** (`budget_disbursements.activity_id` เป็น NOT NULL) เพื่อให้เทียบ "งบตั้ง vs เบิกจริง" ได้ทั้งระดับกิจกรรมและ roll-up ขึ้นเป็นระดับโครงการ
- KPI ยังคงหลักการเดิม: เก็บ "ค่าเป้าหมาย" ที่ระดับโครงการ (`project_kpi`) และ "ค่าจริงตามช่วงเวลา" แยกอีกตาราง (`kpi_progress`) แบบต่อเนื่องไม่มีวันจบ ทำให้กราฟแนวโน้มคำนวณได้โดยไม่ต้องแก้ไขค่าตั้งต้นทับ — ใช้คู่ขนานกับระบบ OKR (`objectives`/`key_results`) ที่มีกรอบเวลาและปิดรอบให้คะแนน สำหรับเป้าหมายระยะสั้นที่ต้องการผลักดัน KPI นั้นให้ดีขึ้น (ข้อ 18)

---

## 4. เมนูและโมดูลของระบบ

0. **ตั้งค่าระบบ / จัดการข้อมูลหลัก** (admin, ผู้ดูแลระบบระดับ สสจ.) — เมนูย่อยสำหรับ **12 ตารางหลัก** ที่ต้อง **เพิ่ม/แก้ไข/ลบได้เอง** ผ่านหน้าจอ (ไม่ใช่แก้ตรงฐานข้อมูล) ดูรายละเอียดในข้อ 10 และ 19 (ระดับความสำคัญโครงการ) — รวมถึงหน้า "ตรวจสอบ KPI ที่รอยืนยัน" จากข้อ 18

1. **Dashboard** — หน้าแรกที่ออกแบบตามระบบดีไซน์ทันสมัยในข้อ 21 (sidebar, stat card แบบมีเทรนด์, กิจกรรมล่าสุด) การ์ดสรุป (จำนวนโครงการ / งบอนุมัติ / เบิกจ่ายแล้ว / กิจกรรมเสร็จสิ้น) + กราฟ 3 ชุด (สถานะโครงการ, ความคืบหน้ากิจกรรม, งบประมาณตามกลุ่มงาน) + กราฟเชิงยุทธศาสตร์เพิ่มเติม (สัดส่วนงบ/กิจกรรมตามระดับการป้องกันโรค ข้อ 22.2, สัดส่วน KPI/Objective ตามระดับผลลัพธ์ Output/Outcome/Impact ข้อ 22.1) — ข้อมูลถูกกรองตามสิทธิ์การมองเห็นของผู้ใช้ (ดูข้อ 6)
2. **โครงการ** — รายการ (ค้นหา/กรองตามสถานะ-กลุ่มงาน-ประเภทโครงการ-**ระดับความสำคัญ**), เพิ่ม/แก้ไข (เลือกประเภท: ทั่วไป / จัดซื้อวัสดุ — **ไม่มีช่องกรอกงบประมาณในฟอร์มนี้**, เลือก**ระดับความสำคัญ/ที่มาของโครงการ**ได้หลายรายการ ข้อ 19, กรอก**สถานการณ์ปัญหา/แหล่งข้อมูลอ้างอิง**และให้**คะแนน BPR**ได้ ข้อ 22.3–22.4, เลือก**สร้างต่อจากโครงการปีก่อน**ได้ถ้าเป็นโครงการต่อเนื่อง ข้อ 22.5), หน้ารายละเอียดแบบ Tab:
   - ข้อมูลทั่วไป (แสดง badge ระดับความสำคัญที่เลือกไว้)
   - กิจกรรม (แต่ละกิจกรรมกรอกงบของตนเอง + เลือก**ระดับการป้องกันโรค**ได้ ข้อ 22.2 + มีสถานะและปุ่มอนุมัติ/ตีกลับของตัวเอง ดูข้อ 5.2 — แสดงสรุป "เสร็จแล้ว x/y กิจกรรม")
   - งบประมาณ (แสดงผลรวมจากทุกกิจกรรม vs เบิกจ่ายจริง — เป็นหน้า "ดูสรุป" ไม่ใช่หน้ากรอกงบตรง)
   - **รายการวัสดุ** (แสดงเฉพาะเมื่อ project_type = จัดซื้อวัสดุ)
   - ประวัติสถานะโครงการ (พร้อมปุ่ม "อนุมัติ" / "ตีกลับ" + ตัวเลือกสถานะอิสระสำหรับผู้มีสิทธิ์สูง — รวมขั้น "ส่งขอโอนเงิน" และจบที่ **"ดำเนินการเรียบร้อย"** ของโครงการเอง ดูข้อ 5.1)
   - KPI/OKR (เลือก **KPI หลัก** ก่อน ตามแผนงาน/โครงการหลักของโครงการ แล้วตั้ง **Objective + Key Results** แบบมีกรอบเวลาได้ — ดูข้อ 18)
3. **กิจกรรม** — รายการรวมทุกโครงการ (เฉพาะที่มีสิทธิ์เห็น), เพิ่ม/แก้ไขต่อโครงการ **(จุดกรอกงบประมาณของโครงการ)**, บันทึกความคืบหน้าเป็น %, บันทึกการเบิกจ่ายจริงต่อกิจกรรม, **ปุ่มอนุมัติ/ตีกลับสถานะกิจกรรม** (รอดำเนินการ → ส่งขอจัดกิจกรรม → ดำเนินการเรียบร้อย ดูข้อ 5.2) พร้อมประวัติสถานะของกิจกรรมนั้น
4. **งบประมาณ** — หน้าสรุปภาพรวม (read-only): อนุมัติ vs เบิกจ่าย vs คงเหลือ ทั้งระดับกิจกรรม/โครงการ/กลุ่มงาน, ดูรายการเบิกจ่ายทั้งหมด (การบันทึกเบิกจ่ายใหม่ทำผ่านหน้ากิจกรรมของโครงการนั้น ๆ)
5. **KPI** — คลังตัวชี้วัดกลาง, ผูก KPI เข้ากับโครงการพร้อมเป้าหมาย, บันทึก/กราฟผลจริงรายครั้ง, Tab **"ภาพรวมทั้งจังหวัด"** เปรียบเทียบทุกโครงการที่ใช้ KPI หลักเดียวกัน (ดูข้อ 17.4)
6. **ผู้ใช้งาน** (admin) — อนุมัติผู้ใช้ใหม่, กำหนดสิทธิ์ และ **กำหนดหน่วยงาน (office)** ที่ใช้กรองการมองเห็นข้อมูล

## 5. Workflow สถานะ — แยกเป็น 2 ระดับ: อนุมัติ+ขอโอนเงิน (15 ขั้น, รายโครงการ) และจัดกิจกรรม (3 ขั้น, รายกิจกรรม)

**เหตุผลที่แยก:** "ส่งขอจัดกิจกรรม" ต้องผูกกับ**กิจกรรม**เพราะ 1 โครงการมีหลายกิจกรรมที่จัดคนละเวลากัน ส่วน **"ขอโอนเงิน" เป็นรายโครงการ** เพราะเงินงบประมาณที่อนุมัติแล้วโอนเข้าหน่วยงานเป็นก้อนเดียวต่อโครงการ ไม่ได้แยกโอนทีละกิจกรรม จึงยังอยู่ใน chain ของโครงการเหมือนเดิม พร้อมสถานะปิดท้าย "ดำเนินการเรียบร้อย" ของโครงการเอง (คนละสถานะกับ "ดำเนินการเรียบร้อย" ของแต่ละกิจกรรม)

### 5.1 Chain ที่ 1 — อนุมัติโครงการ + ขอโอนเงิน (`project_status`, 15 ขั้น)
```
ยังไม่ดำเนินการ → เสนอโครงการ → หัวหน้างานตรวจสอบ ⇄ (ให้แก้ไข) →
ผช.สสอ.ตรวจสอบ ⇄ (ให้แก้ไข) → สสอ.ลงนาม → ส่ง รพ. → รพ.ตรวจสอบ/ลงนาม ⇄ (ให้แก้ไข) →
ส่ง สสจ. ⇄ (ให้แก้ไข) → อนุมัติโครงการ → **ส่งขอโอนเงิน (รายโครงการ)** → **ดำเนินการเรียบร้อย** [สิ้นสุด chain โครงการ]
```
ทุกครั้งที่เปลี่ยนสถานะ ระบบบันทึกลง `project_tracking_log` และอัปเดต `projects.status_id` ให้ตรงกับสถานะล่าสุดเสมอ — "ส่งขอโอนเงิน" ทำได้ครั้งเดียวต่อโครงการ ไม่ต้องรอให้ทุกกิจกรรมเสร็จก่อน (เงินโอนไว้ล่วงหน้าเพื่อให้แต่ละกิจกรรมเบิกใช้ได้)

### 5.2 Chain ที่ 2 — จัดกิจกรรม (`activity_status`, 3 ขั้น, ผูกกับกิจกรรมแต่ละรายการ)
```
รอดำเนินการ → ส่งขอจัดกิจกรรม → **ดำเนินการเรียบร้อย** [สถานะสุดท้ายของกิจกรรมนั้น]
```
ผูกกับ**กิจกรรมแต่ละรายการ**ไม่ใช่โครงการ — โครงการเดียวกันจึงมีกิจกรรมที่สถานะต่างกันได้พร้อมกัน (เช่น กิจกรรม A "ดำเนินการเรียบร้อย" แล้ว ขณะที่กิจกรรม B ยัง "รอดำเนินการ") ทุกครั้งที่เปลี่ยนสถานะ ระบบบันทึกลง `activity_tracking_log` และอัปเดต `activities.status_id`
- ระบบควรกันไม่ให้เลื่อนสถานะกิจกรรมเป็น "ส่งขอจัดกิจกรรม" ได้ ถ้าโครงการแม่ยังไม่ถึงสถานะ "อนุมัติโครงการ" เป็นอย่างน้อย (ตรวจสอบ `projects.status_id` ก่อนอนุญาตเปลี่ยนสถานะกิจกรรม) — ไม่จำเป็นต้องรอถึง "ส่งขอโอนเงิน"/"ดำเนินการเรียบร้อย" ของโครงการก่อน เพราะกิจกรรมสามารถเริ่มดำเนินการคู่ขนานกับการขอโอนเงินได้
- หน้ารายละเอียดโครงการ (ข้อ 4) อาจแสดงตัวช่วย "ความคืบหน้ากิจกรรมโดยรวม" เช่น "เสร็จแล้ว 3/5 กิจกรรม" จากการนับ `activities.status_id` ที่เป็น "ดำเนินการเรียบร้อย" — เป็นตัวช่วยแสดงผล ไม่ใช่สถานะใหม่ของโครงการ และ**ไม่ผูกกับสถานะ "ดำเนินการเรียบร้อย" ของโครงการโดยอัตโนมัติ** (ปิดโครงการเป็นการตัดสินใจแยกต่างหากของผู้รับผิดชอบโครงการ)

### 5.3 ปุ่มเปลี่ยนสถานะ (ใช้รูปแบบเดียวกันทั้ง 2 chain)
1. **ปุ่ม "อนุมัติ" / "ตีกลับ"** (ใช้งานทั่วไป ตามลำดับขั้น) — "อนุมัติ" เลื่อนไปที่ `next_status_id` ของสถานะปัจจุบัน (ในตาราง `project_status` หรือ `activity_status` แล้วแต่ระดับ), "ตีกลับ" เลื่อนไปที่ `return_status_id` ปุ่มจะแสดง/ซ่อนตามสิทธิ์ของผู้ใช้ในแต่ละขั้น (ผู้ตรวจสอบระดับ สสอ./รพ. เห็นเฉพาะโครงการในกลุ่มงานตนเอง ไม่ต้องเป็นผู้สร้างโครงการนั้น — ดูกฎละเอียดในข้อ 6) และจะไม่แสดงปุ่มที่ไม่มีค่าปลายทาง (เช่น สถานะสุดท้าย "ดำเนินการเรียบร้อย" ทั้งสองระดับ ไม่มี `next_status_id`)
2. **เลือกสถานะได้อิสระ (dropdown)** — สำหรับผู้มีสิทธิ์สูง (สสจ./admin) กรณีต้องข้ามขั้นหรือแก้ไขสถานะย้อนหลัง ใช้ได้ทั้งระดับโครงการและระดับกิจกรรม

การเปลี่ยนสถานะทุกครั้ง (ไม่ว่าจากปุ่มหรือ dropdown, ระดับโครงการหรือกิจกรรม) จะ trigger การส่งอีเมลแจ้งเตือนตามข้อ 6.1

## 6. สิทธิ์การใช้งาน (Role-based) และการมองเห็นข้อมูล

**หลักการสำคัญที่ยืนยันแล้ว — แยก 2 สิทธิ์ออกจากกันชัดเจน เพราะเป็นคนละเรื่องกัน:**
1. **แก้ไขเนื้อหาโครงการ/กิจกรรม** (ชื่อ, วัตถุประสงค์, งบประมาณ, กิจกรรม ฯลฯ) — **ผู้ใช้ทุกระดับแก้ไขได้เฉพาะโครงการที่ตนเองเป็นผู้สร้าง** (`created_by = current_user_id`) เท่านั้น ไม่ว่าจะอยู่ level ไหนก็ตาม (หน่วยบริการ, สสอ., รพ., สสจ.) — นี่คือสิทธิ์พื้นฐานของ **"ผู้เสนอโครงการ"** ที่ผู้ใช้ทุกคนมีเหมือนกันหมด
2. **เปลี่ยนสถานะโครงการ** (ปุ่ม "อนุมัติ"/"ตีกลับ"/เลือกสถานะอิสระ — ข้อ 5) — เป็นสิทธิ์เพิ่มเติมสำหรับ**ผู้ตรวจสอบ**เท่านั้น (level = สสอ. หรือ รพ.) โดยจำกัดเฉพาะ**โครงการที่ `owner_office_id` ตรงกับกลุ่มงานของผู้ตรวจสอบเอง** — **ไม่ต้องเป็นผู้สร้างโครงการนั้นก็เปลี่ยนสถานะได้** (ต่างจากข้อ 1 ที่บังคับต้องเป็นผู้สร้าง) เพราะบทบาทนี้คือการตรวจสอบงานของคนอื่นในกลุ่มงานเดียวกัน ไม่ใช่แก้ไขเนื้อหาเอง

| Level | เห็นโครงการของ | แก้ไขเนื้อหาโครงการ/กิจกรรม | เปลี่ยนสถานะ (อนุมัติ/ตีกลับ) | เลือกสถานะอิสระ | จัดการผู้ใช้ |
|---|---|---|---|---|---|
| หน่วยบริการ | เฉพาะหน่วยบริการของตนเอง (`owner_facility_id` ตรงกับ `users.facility_id` ข้อ 23.3) | ✅ **เฉพาะที่ตนเองสร้าง** (`created_by = self`) | ❌ | ❌ | ❌ |
| **สสอ. (ผู้ตรวจสอบ)** | หน่วยงาน/กลุ่มงานในสังกัด + ของตนเอง | ✅ **เฉพาะที่ตนเองสร้าง** (เหมือนทุก level — ไม่ใช่แค่เพราะเป็น สสอ. แล้วแก้ของคนอื่นได้) | ✅ **เฉพาะโครงการที่ `owner_office_id` = กลุ่มงานของตนเอง** (ไม่ต้องเป็นผู้สร้าง) | ❌ | ❌ |
| **รพ. (ผู้ตรวจสอบ)** | หน่วยงาน/กลุ่มงานในสังกัด + ของตนเอง | ✅ **เฉพาะที่ตนเองสร้าง** (เหมือนกัน) | ✅ **เฉพาะโครงการที่ `owner_office_id` = กลุ่มงานของตนเอง** (ไม่ต้องเป็นผู้สร้าง) | ❌ | ❌ |
| สสจ. | ทุกหน่วยงาน | ✅ เฉพาะที่ตนเองสร้าง | ✅ ทุกกลุ่มงาน (ไม่จำกัดแค่กลุ่มงานตนเองเหมือนสสอ./รพ.) | ✅ | ❌ |
| admin | ทุกหน่วยงาน | ✅ ทุกโครงการ (ข้อยกเว้นเดียว — เพื่อแก้ไขข้อมูลผิดพลาด/ดูแลระบบ) | ✅ ทุกขั้น | ✅ | ✅ |

**ข้อสังเกตสำคัญ:** ระดับ สสอ./รพ. **"เห็น" ได้กว้างกว่าที่ "แก้ไขเนื้อหา" ได้** — เห็นทั้งกลุ่มงานในสังกัดเพื่อใช้ตรวจสอบ/เปลี่ยนสถานะ แต่แก้ไขเนื้อหาตรง ๆ ได้เฉพาะโครงการของตัวเองเหมือนทุกคน การแยกแบบนี้ทำให้ "ตรวจสอบ" กับ "แก้ไขแทนเจ้าของ" ไม่ปนกัน และมีร่องรอย (audit trail ข้อ 15) ชัดเจนว่าใครเป็นคนแก้เนื้อหา ใครเป็นคนอนุมัติสถานะ

การกรองข้อมูลและตรวจสอบสิทธิ์ทำที่ระดับ query/business logic ฝั่ง server เสมอ ไม่ใช่ซ่อนปุ่มด้วย JavaScript ฝั่ง client เพื่อความปลอดภัย ตัวอย่างเงื่อนไข:
- **ปุ่มแก้ไขเนื้อหา** แสดงเมื่อ `projects.created_by = session.user_id` เท่านั้น (ทุก level รวมถึง สสอ./รพ./สสจ. — ยกเว้น admin)
- **ปุ่มอนุมัติ/ตีกลับ** แสดงเมื่อ (`level IN ('สสอ','รพ')` AND `projects.owner_office_id = session.user_office_id`) OR (`level IN ('สสจ','admin')`)

### 6.1 การแจ้งเตือนทางอีเมล

| เหตุการณ์ | ผู้รับ | เงื่อนไข |
|---|---|---|
| สถานะโครงการเปลี่ยน | เจ้าของโครงการ (ผู้สร้าง) + ผู้รับผิดชอบขั้นถัดไป | ทุกครั้งที่ `project_tracking_log` มีแถวใหม่ |
| ใกล้ครบกำหนดดำเนินการ | เจ้าของโครงการ + ผู้รับผิดชอบ | Cron รายวันเช็ค `planned_end_date` ล่วงหน้า 7 วัน และ 1 วัน (เก็บใน `notification_log` เพื่อกันส่งซ้ำ) |

ใช้ PHP `PHPMailer` (SMTP) หรือ `mail()` ตามที่ Hosting รองรับ, งานแจ้งเตือนใกล้ครบกำหนดรันผ่าน Cron Job เรียกสคริปต์ PHP ทุกวัน

## 7. เทคโนโลยีที่ใช้

- **Backend:** PHP 8.x, PDO (MySQL), session-based auth ผ่าน **3 ช่องทาง: Google OAuth 2.0, LINE Login SDK, และ Username/Password (bcrypt ผ่าน `password_hash()`/`password_verify()` ของ PHP)**, PHPMailer (แจ้งเตือนอีเมล + ลืมรหัสผ่าน), **PhpSpreadsheet** (export รายงาน .xlsx ข้อ 11) และ **PhpWord** (พิมพ์หนังสือราชการจากเทมเพลต .docx ข้อ 17.5) + Cron job สำหรับแจ้งเตือนใกล้ครบกำหนด
- **Database:** MySQL 8 / InnoDB / utf8mb4
- **Frontend:** Bootstrap 5 (ธีมปรับแต่งทับ default ทั้งหมด — ดูข้อ 21), Google Fonts "Prompt"/"IBM Plex Sans Thai", Bootstrap Icons (หรือ Phosphor/Tabler Icons), DataTables (ตารางค้นหา/เรียง/แบ่งหน้า, ตั้งค่าเป็นภาษาไทยทั้งหมด), Chart.js (กราฟแดชบอร์ด, สีตาม palette ข้อ 21.1), **flatpickr + locale ไทย (ปรับแสดงปีเป็น พ.ศ.)** สำหรับทุกช่องกรอกวันที่ในระบบ (ดูข้อ 13)
- **โครงสร้างโฟลเดอร์:** แยกตามโมดูล (`projects/`, `activities/`, `budget/`, `kpi/`, `tracking/`, `users/`) เพื่อให้ดูแลและขยายง่าย

## 8. แนวทางการนำเข้าข้อมูลเดิม (Data Migration)

ข้อมูลจริงในไฟล์ Excel แมปเข้าตารางได้ครบ:
- ชีต `DATA` (117 โครงการ) → ตาราง `projects` โดยจับคู่ข้อความ (ชื่อกลุ่มงาน, รหัสแผน, รหัสโครงการหลัก ฯลฯ) กับตาราง master ผ่าน SELECT ... WHERE code/name เพื่อไม่ยึดติด ID; ตั้ง `project_type = 'general'` เป็นค่าเริ่มต้นให้ทุกแถวที่นำเข้า (ผู้ใช้ปรับเป็น "จัดซื้อวัสดุ" ภายหลังได้), ตั้ง `owner_office_id` จากคอลัมน์ "หน่วยงานที่รับผิดชอบ"
- ชีต `กิจกรรม` (185 รายการ) เชื่อมกับโครงการผ่าน `pjCode` ซึ่งตรงกับ `projectCode` ใน DATA ราว 94% ของแถว
- ชีต `tracking` (458 รายการ) เชื่อมกับโครงการผ่านรหัสเดียวกัน ใช้เติม `project_tracking_log` ตามปกติสำหรับทุกสถานะ **ยกเว้นเฉพาะ** แถวที่ค่าสถานะเดิมเป็น `14d8` (ส่งขอจัดกิจกรรม — สถานะเดียวที่ย้ายไปเป็นระดับกิจกรรม ดูข้อ 5) ซึ่ง map เข้า `project_tracking_log` ตรง ๆ ไม่ได้เพราะข้อมูลเดิมไม่ได้ระบุว่าเป็นกิจกรรมไหน — แนะนำให้ import เป็น `project_tracking_log` ปกติไปก่อนตามข้อมูลเดิม (เพื่อไม่ให้ประวัติหาย) แล้วให้ผู้ใช้ไปสร้างสถานะกิจกรรมจริงเพิ่มเองภายหลังผ่านหน้ากิจกรรม ส่วน `14d9`/`15d9` นำเข้าเป็น `project_tracking_log` ได้ตรง ๆ ตามปกติ เพราะยังเป็นสถานะระดับโครงการเหมือนเดิม
- ชีต `materail` (ข้อมูลเดิม) ไม่นำเข้าอัตโนมัติ เนื่องจากไม่ได้ผูกกับ `projectCode` ในไฟล์ต้นฉบับ — ผู้ใช้จะบันทึกรายการวัสดุใหม่ผ่านโครงการประเภท "จัดซื้อวัสดุ" โดยตรง

## 9. สรุปการตัดสินใจ (ยืนยันแล้ว)

| ประเด็น | ผลสรุป |
|---|---|
| สิทธิ์การมองเห็นข้อมูล | หน่วยบริการเห็นเฉพาะโครงการของหน่วยตนเอง ผ่าน `owner_office_id` / `users.office_id` |
| การอนุมัติหลายระดับ | ปุ่ม "อนุมัติ / ตีกลับ" ตามลำดับขั้น + เลือกสถานะอิสระได้สำหรับผู้มีสิทธิ์สูง |
| การแจ้งเตือน | อีเมลเมื่อสถานะเปลี่ยน และแจ้งเตือนล่วงหน้าเมื่อใกล้ครบกำหนด (7 วัน / 1 วัน) |
| โมดูลวัสดุ | ตัดโมดูลอิสระออก แยกเป็น **ประเภทโครงการ**: ทั่วไป / จัดซื้อวัสดุ (ใช้ตาราง `project_materials` ผูกกับโครงการโดยตรง) |
| โครงสร้างงบประมาณ | ตั้งงบที่ระดับ **กิจกรรม** เท่านั้น (`activities.budget_plan`) — งบของโครงการ (`projects.budget_approved`) เป็นผลรวมอัตโนมัติของทุกกิจกรรม ไม่มีช่องกรอกตรงในฟอร์มโครงการ |
| ข้อมูลหลักที่ต้องแก้ไขได้ | 12 ตาราง (ดูข้อ 10, 19, และ 23) เปิดให้ admin เพิ่ม/แก้ไข/ลบผ่านหน้าจอ ไม่ต้องแก้ที่ฐานข้อมูลโดยตรง |
| ข้อมูลเพิ่มจากตัวอย่างรายงาน | `fiscal_year_id` (ตาราง `fiscal_years` ใหม่), `network_id` ที่โครงการ; ย้าย `budget_source_id` ไปที่กิจกรรม + เพิ่ม `objective`, `kpi_target_text`, `target_area`, `responsible_person`, `activity_months` ที่กิจกรรม; เพิ่มแหล่งงบ `สป.`/`กรม` และ `budget_sources.category` (ดูข้อ 11) |
| การ Login | เปิด **3 ช่องทาง: Google OAuth 2.0, LINE Login, และ Username/Password** — ช่องทาง Username/Password **สมัครได้เอง** (ต้องยืนยันอีเมลก่อน + มี CAPTCHA กันสแปม) หรือ admin สร้างให้โดยตรงก็ได้ มีฟีเจอร์ลืมรหัสผ่าน + ล็อกบัญชีชั่วคราวเมื่อ login ผิดหลายครั้ง — ผู้ใช้ใหม่ทุกช่องทางยัง**ต้องรออนุมัติจาก admin ก่อน** เหมือนเดิม (ดูข้อ 12) |
| ความสมบูรณ์เพิ่มเติม | เพิ่มตาราง `fiscal_years` (ควบคุมปีงบประมาณ), ปรับ `project_status` ให้มี `next_status_id`/`return_status_id` ชัดเจน, เพิ่ม `attachments` (ไฟล์แนบ) |
| ติดตามการแก้ไขข้อมูล (Audit Trail) | แยก `audit_log` (หัวรายการ: ใคร/ตารางไหน/เมื่อไร) + `audit_log_details` (ระดับฟิลด์: แก้อะไร จากค่าไหนเป็นค่าไหน) ครอบคลุมทั้งข้อมูลธุรกรรมและข้อมูลหลัก แสดงผลเป็น Tab "ประวัติการแก้ไข" ภาษาไทยอ่านง่าย (ดูข้อ 15) |
| รูปแบบวันที่ | เก็บ ค.ศ. ในฐานข้อมูลเสมอ แสดงผล **พ.ศ. 4 หลัก** ทุกจุดในระบบ ผ่าน helper กลาง + Thai datepicker (ดูข้อ 13) |
| ข้อความแจ้งเตือน/สถานะโหลด | ภาษาไทยล้วน เข้าใจง่าย มีชุดคำมาตรฐานกลาง ไม่แสดง error ทางเทคนิคให้ผู้ใช้เห็น (ดูข้อ 14) |
| สถานะ "ส่งขอจัดกิจกรรม" / "ขอโอนเงิน" | **เฉพาะ "ส่งขอจัดกิจกรรม"** ย้ายไปผูกกับ**กิจกรรม** (ตาราง `activity_status` ใหม่ 3 ขั้น: รอดำเนินการ → ส่งขอจัดกิจกรรม → **ดำเนินการเรียบร้อย**) เพราะแต่ละกิจกรรมจัดคนละเวลากัน — ส่วน **"ขอโอนเงิน" ยังเป็นรายโครงการเหมือนเดิม** เพราะโอนเงินเป็นก้อนเดียวต่อโครงการ โครงการจึงคง 15 สถานะ (`project_status`) จบที่ "ดำเนินการเรียบร้อย" ของโครงการเอง (ดูข้อ 5) |
| ปุ่มคำอธิบายการกรอกข้อมูล | ทุกฟิลด์ที่อาจสับสน (คำนวณอัตโนมัติ, ถูกกรองแบบ cascading, ศัพท์เฉพาะราชการ) มีไอคอน (ⓘ) กด popover ดูคำอธิบายภาษาไทยได้ทันที ใช้ dictionary กลางร่วมกับ label และประวัติแก้ไข (ดูข้อ 16) |
| ข้อเสนอแนะเพิ่มเติม (ยืนยันแล้ว) | ช่องค้นหากลางทุกโมดูล, เตือนชื่อโครงการซ้ำ/คล้ายกันแบบ advisory, กฎตรวจสอบอัตโนมัติ (งบ/ขั้นตอน) แยกบล็อก/เตือน, มุมมอง KPI ภาพรวมทั้งจังหวัด, พิมพ์หนังสือราชการอัตโนมัติจากเทมเพลต, และ index ฐานข้อมูลจุดสำคัญ (ดูข้อ 17) |
| ตัวชี้วัดย่อยที่ผู้ใช้กรอก → OKR | เปลี่ยนจาก "KPI ย่อยไม่มีวันจบ" เป็น **OKR เต็มรูปแบบ**: `objectives` (มีกรอบเวลา/ไตรมาส ผูกกับ KPI หลักเสมอ ตรวจสอบสัมพันธ์กับแผนงาน/โครงการหลักเหมือนเดิม) → `key_results` (มีทิศทางเพิ่ม/ลด, คะแนนตอนปิดรอบ) → `key_result_progress` — ปิดรอบแล้วให้คะแนนอัตโนมัติ ห้ามแก้ไขย้อนหลัง ต้องเริ่มรอบใหม่แทน (ดูข้อ 18) |
| แหล่งอ้างอิงของ Key Result | เพิ่มตาราง `kpi_references` (1:N ต่อ Key Result) ใส่ลิงก์แหล่งที่มาได้หลายรายการ พร้อมคำอธิบายสั้น กดไอคอนลิงก์เปิดแท็บใหม่ได้จากทุกจุดที่แสดง Key Result (ดูข้อ 18.6) |
| ระดับความสำคัญของโครงการ | เพิ่มตาราง `priority_levels` (ตั้งค่าได้เอง เช่น นโยบายระดับเขต/จังหวัด, ประเด็น พชอ., ปัญหาสุขภาพในพื้นที่) ผูกกับโครงการแบบ M:N ผ่าน `project_priority_levels` เพราะ 1 โครงการเข้าข่ายได้มากกว่า 1 ระดับ ใช้เป็นตัวกรองในรายการโครงการ/Dashboard ได้ด้วย (ดูข้อ 19) |
| บังคับกรอกข้อมูล | ทุกฟิลด์จำเป็นบังคับกรอกทั้ง client/server พร้อมข้อความไทย — **ยกเว้นงบประมาณกิจกรรม** (`activities.budget_plan`) ที่บังคับมีค่าเสมอแต่อนุญาตเป็น 0 (`NOT NULL DEFAULT 0`, pre-fill 0 ในฟอร์ม) ส่วนจำนวนเงินเบิกจ่ายจริง (`budget_disbursements.amount`) ต้องมากกว่า 0 เท่านั้น (ดูข้อ 20) |
| ดีไซน์ Dashboard ทันสมัย | ปรับธีม Bootstrap ทับด้วย palette สี Indigo/Violet + ฟอนต์ "Prompt"/"IBM Plex Sans Thai" + sidebar ซ้าย + stat card แบบมีไอคอน gradient/trend indicator + skeleton loading + empty state ที่ออกแบบแล้ว ให้ดูเป็น SaaS dashboard มืออาชีพ ไม่ใช่ Bootstrap default (ดูข้อ 21) |
| หน่วยบริการ (Health Facilities) | เพิ่มตาราง `health_facilities` (รพ.สต./ศสช./รพ. จริง แยกจาก `departments` ที่เป็นกลุ่มงานภายใน สสอ.) ผูกกับโครงการ/กิจกรรมแบบ M:N (`project_facilities`/`activity_facilities`) และแก้ไขสิทธิ์การมองเห็นของผู้ใช้ระดับหน่วยบริการให้อิง `owner_facility_id`/`users.facility_id` แทน `office_id` ที่ผิดระดับเดิม (ดูข้อ 23) |
| สิทธิ์แก้ไข vs สิทธิ์อนุมัติ | แยกชัดเจน: **แก้ไขเนื้อหาโครงการ/กิจกรรม** — ทุก level ทำได้เฉพาะที่ตนเองสร้าง (`created_by = self`) เท่านั้น ไม่มีข้อยกเว้นตาม level; **เปลี่ยนสถานะ (อนุมัติ/ตีกลับ)** — เป็นสิทธิ์เพิ่มของ "ผู้ตรวจสอบ" (level สสอ./รพ.) จำกัดเฉพาะโครงการที่ `owner_office_id` ตรงกับกลุ่มงานตนเอง โดยไม่ต้องเป็นผู้สร้าง (ดูข้อ 6) |
| มุมมองนักยุทธศาสตร์สาธารณสุข | เพิ่ม 5 เรื่อง: (1) `result_level` จำแนก Output/Outcome/Impact ใน `kpi_master`/`key_results`, (2) `activities.prevention_level` ปฐม/ทุติย/ตติยภูมิ, (3) `projects.situation_analysis`/`situation_data_source` อ้างอิงข้อมูลระบาดวิทยา **+ `priority_levels` อย่างน้อย 1 รายการ — บังคับกรอกก่อนเปลี่ยนสถานะเป็น "เสนอโครงการ" (บันทึกร่างได้โดยยังไม่ครบ)**, (4) `project_priority_scores` คะแนน BPR ตามสูตร Hanlon (ยังไม่บังคับ), (5) `projects.parent_project_id` โยงความต่อเนื่องข้ามปีงบประมาณ (ดูข้อ 22) |

## 10. ตารางหลักที่ต้องจัดการได้ในระบบ (Editable Master Data)

จากชีตต้นฉบับ 8 ชีตที่ระบุว่าเป็น "ข้อมูลที่ต้องแก้ไขได้และสำคัญต่อการนำเข้า" บวกกับ `fiscal_years`, `activity_status`, `priority_levels`, และ `health_facilities` ที่เพิ่มใหม่เพื่อความสมบูรณ์ (รวม 12 ตาราง) ออกแบบเป็นเมนูย่อยภายใต้ **ตั้งค่าระบบ / จัดการข้อมูลหลัก** โดยแต่ละตารางมีหน้าจอ รายการ + เพิ่ม + แก้ไข + ลบ (ลบไม่ได้ถ้ามีโครงการอ้างอิงอยู่ — ป้องกันด้วย FK constraint และแจ้งเตือนก่อนลบ)

| ชีตต้นฉบับ | ตารางในระบบ | โครงสร้างข้อมูล | หมายเหตุการออกแบบ |
|---|---|---|---|
| — (ใหม่) | `fiscal_years` | year_be, label, start_date, end_date, is_active | admin เปิดปีงบประมาณใหม่ทุกปี (เช่น เปิด 2570 ก่อนเริ่มปี) และปิดปีเก่าเพื่อไม่ให้สร้างโครงการใหม่ผิดปีย้อนหลัง |
| `KPI` | `kpi_master` | code, name, **main_project_id** (FK `main_projects`, nullable) | รายการตัวชี้วัดกลาง 38 รายการ — หน้าแก้ไขมีช่องเลือก "โครงการหลักที่สังกัด" เพื่อให้ cascading dropdown ทำงานได้ (ข้อ 10.1) |
| `Copy of KPI` | *(ไม่ใช่ตารางแยกอีกต่อไป)* | ใช้เป็น **แหล่งข้อมูลตอนนำเข้า** เท่านั้น | เดิมเป็นตารางข้อความ "แผนงาน → โครงการ → ตัวชี้วัด" 38 แถว ในระบบใหม่ไม่ต้องเก็บเป็นตารางแยก — ใช้จับคู่ชื่อครั้งเดียวตอน migration เพื่อเติมค่า `main_projects.plan_id` และ `kpi_master.main_project_id` ให้ครบ (ดูข้อ 10.1) |
| `pjCode` | `main_projects` | code (PJ1–PJ29), name, **plan_id** (FK `plans`) | โครงการหลักระดับกระทรวง — หน้าแก้ไขมีช่องเลือก "แผนงานที่สังกัด" |
| `planCode` | `plans` | code (Plan1–Plan13), name | แผนงานหลัก — จุดเริ่มต้นบนสุดของลำดับชั้น cascading |
| `mophEx` | `moph_excellence` | code (1–5), name | ประเด็น Excellence ของกระทรวง |
| `StrategicIssues` | `strategic_issues` | name | ประเด็นยุทธศาสตร์ (9 ข้อ) |
| `สถานะโครงการ` | `project_status` | code, name, **sort_order**, **next_status_id** (FK ตัวเอง), **return_status_id** (FK ตัวเอง) | **คง 15 ขั้น** (เดิม 17, ตัดออกแค่ "ส่งขอจัดกิจกรรม") จบที่ "ดำเนินการเรียบร้อย" รวมขั้น "ส่งขอโอนเงิน" ที่ยังเป็นรายโครงการ — หน้าแก้ไขต้องมีช่องเลือก "สถานะถัดไปเมื่ออนุมัติ" และ "สถานะเมื่อตีกลับ" อย่างชัดเจน (ไม่ใช้แค่ sort_order คำนวณเอา) เพราะปุ่ม "อนุมัติ/ตีกลับ" (ข้อ 5.1) อ้างอิงค่านี้โดยตรง จึงต้องระวังเป็นพิเศษเวลาแก้ไข/แทรกสถานะใหม่ |
| — (ใหม่ — แยกจากสถานะโครงการเดิม) | `activity_status` | code, name, sort_order, next_status_id, return_status_id | สถานะการดำเนินกิจกรรม **3 ขั้น** (รอดำเนินการ → ส่งขอจัดกิจกรรม → ดำเนินการเรียบร้อย) — เฉพาะ "ส่งขอจัดกิจกรรม" ที่ย้ายมาจากสถานะโครงการเดิม เพราะแต่ละกิจกรรมในโครงการเดียวกันจัดงานไม่พร้อมกัน ส่วน "ขอโอนเงิน" ยังอยู่ที่ระดับโครงการ (ดูข้อ 5.2) |
| `Template_โครงการ` | `project_templates` (ใหม่) | template_name, department_id, objective, strategic_issue_id, goal, kpi_text, target_text, strategy_text, suggested_budget_note, duration_text, responsible_person, expected_outcome | เดิมเป็นชีตเปล่าไว้กรอกตั้งต้น ในระบบใหม่ออกแบบเป็น **คลังแม่แบบโครงการ** ที่ admin สร้างไว้ล่วงหน้า (เช่น แม่แบบโครงการส่งเสริมสุขภาพแม่และเด็ก) — ตอนสร้างโครงการใหม่ ผู้ใช้เลือก "ใช้แม่แบบ" แล้วระบบดึงค่ามาเติมในฟอร์มให้อัตโนมัติ (แก้ไขได้ก่อนบันทึก) ช่วยลดเวลากรอกโครงการที่คล้ายกันซ้ำ ๆ; `suggested_budget_note` เป็นข้อความอ้างอิงเท่านั้น ไม่ผูกกับ `budget_approved` จริง เพราะงบจริงมาจากผลรวมกิจกรรม (ข้อ 9) |
| — (ใหม่ — ข้อ 19) | `priority_levels` | code, name, sort_order, is_active | ระดับความสำคัญ/ที่มาของโครงการ (นโยบายระดับเขต/จังหวัด, ประเด็น พชอ., ปัญหาสุขภาพในพื้นที่ ฯลฯ) — ตั้งค่าเองได้ทั้งหมด ไม่ hardcode เพราะนโยบายเปลี่ยนได้ทุกปี |
| — (ใหม่ — ข้อ 23) | `health_facilities` | code, name, facility_type, network_id, tambon, is_active | รายชื่อหน่วยบริการจริง (รพ.สต./ศสช./รพ.) — แก้ไข/เพิ่มได้เมื่อมีการยุบ/ควบรวม/เปิดหน่วยบริการใหม่ ใช้ผูกกับสิทธิ์การมองเห็นของผู้ใช้ระดับหน่วยบริการด้วย (ข้อ 23.3) |

**หลักการสิทธิ์:** เมนู "จัดการข้อมูลหลัก" ทั้ง 12 รายการเปิดให้เฉพาะ role `admin` (และอาจเปิดให้ สสจ. ได้บางตารางถ้าต้องการ เช่น `project_templates`) ส่วนผู้ใช้ระดับหน่วยบริการ/สสอ./รพ. เห็นเป็น dropdown อย่างเดียวในฟอร์มโครงการ ไม่มีสิทธิ์แก้ไขตารางหลักเหล่านี้

### 10.1 Cascading Dropdown: แผนงาน → โครงการหลัก → KPI

**โครงสร้างที่รองรับ:** เพิ่มคอลัมน์ FK ตรงในตารางแทนการสร้างตาราง mapping แยก เพื่อให้เป็นลำดับชั้นเดียวชัดเจน (1 แผนงาน → หลายโครงการหลัก → หลาย KPI):
- `main_projects.plan_id` → FK `plans.id`
- `kpi_master.main_project_id` → FK `main_projects.id` (เป็น NULL ได้ สำหรับ KPI ที่ไม่ผูกกับโครงการหลักเฉพาะ เช่น `KPI00`)

**พฤติกรรมในฟอร์มสร้าง/แก้ไขโครงการ** (ใช้ JavaScript fetch ไป endpoint เช่น `/api/main_projects.php?plan_id=..` และ `/api/kpis.php?main_project_id=..` โดยไม่โหลดหน้าใหม่):
1. ผู้ใช้เลือก **แผนงาน** → dropdown "โครงการหลัก" โหลดใหม่ให้เหลือเฉพาะแถวที่ `plan_id` ตรงกัน (ถ้ายังไม่เลือกแผนงาน ให้โครงการหลักว่าง/ปิดใช้งานไว้ก่อน)
2. ผู้ใช้เลือก **โครงการหลัก** → รายการ **KPI หลัก** ที่แสดงให้เลือกผูกกับโครงการ (หน้า Tab "KPI/OKR" ของโครงการ) กรองเฉพาะที่ `main_project_id` ตรงกัน (ตอนนี้ `kpi_master` มีแต่ KPI หลักแล้ว ไม่มี KPI ย่อยปนอยู่ในตารางนี้อีกต่อไป — การตั้ง Objective/Key Result เป็นขั้นถัดไปหลังเลือก KPI หลักแล้ว ดูข้อ 18) + เพิ่มตัวเลือก "KPI อื่น ๆ (ไม่ผูกกับโครงการหลักนี้)" ไว้เผื่อกรณีพิเศษ ไม่บังคับปิดทางเลือกทั้งหมด
3. ถ้าโครงการหลักที่เลือกยังไม่มี KPI ผูกไว้เลย (ข้อมูลนำเข้ายังไม่ครบ) ให้ fallback แสดง KPI ทั้งหมดพร้อมข้อความแจ้งเตือน "โครงการหลักนี้ยังไม่ได้กำหนด KPI ที่เกี่ยวข้อง"

**การนำเข้าข้อมูลเริ่มต้น (จากชีต `Copy of KPI`):** จับคู่ข้อความ "โครงการ" ในชีตนี้ (39 แถว) กับชื่อใน `main_projects` (29 รายการ) — ชื่อไม่ตรงกันเป๊ะทุกตัวอักษร (เช่น "โครงการพัฒนาและสร้างศักยภาพคนไทยทุกกลุ่มวัย" ในชีต vs "โครงการที่ 1 โครงการพัฒนาและสร้างศักยภาพคนไทย ทุกกลุ่มวัย" ใน pjCode) จึงต้อง **จับคู่แบบ fuzzy match (เทียบคำคล้ายกัน) แล้วให้ admin ตรวจทานผ่านหน้าจัดการ `main_projects`/`kpi_master` อีกรอบก่อนเผยแพร่ใช้งานจริง** ไม่ควรเชื่อผลจับคู่อัตโนมัติ 100%

## 11. โมดูลรายงาน (Reports) — จากตัวอย่างไฟล์ที่แนบ

ตรวจสอบไฟล์ตัวอย่าง 2 ไฟล์แล้วพบว่าเป็นรายงานทางการ 2 รูปแบบที่หน่วยงานใช้อยู่ ต้องออกแบบให้ระบบสร้างได้ตรงตามฟอร์แมตนี้ (export เป็น Excel/PDF)

### 11.1 รายงานที่ 1 — สรุปโครงการและงบประมาณตามแผนปฏิบัติการ (ไฟล์ `00_สรุปหน้างบ_ปงบ_2569.xls`)
โครงสร้าง: **หัวรายงาน** (ปีงบประมาณ + เครือข่ายบริการสุขภาพ) → รายการยุบ/ขยายตามลำดับชั้น **Excellence → แผนงาน (subtotal) → โครงการหลัก (subtotal) → โครงการย่อย (แถวรายละเอียด)** → คอลัมน์งบประมาณ **pivot ตามแหล่งงบ** (สป., กรม, PP, CUP, กองทุนตำบล, กองทุนเอดส์, กองทุนวัณโรค, รวม) → คอลัมน์หน่วยงานที่รับผิดชอบ

**สิ่งที่ต้องเก็บเพิ่มเพื่อสร้างรายงานนี้ได้ (สรุปแล้วอยู่ในตารางข้อ 3 ด้านบน):**
- `projects.fiscal_year_id` (join `fiscal_years.year_be`), `projects.network_id` — ใช้กรอง/ขึ้นหัวรายงาน
- `budget_sources.category` และรายการ `สป.`/`กรม` ที่ยังไม่มี — ใช้ pivot เป็นคอลัมน์
- งบประมาณต้องรวมจาก `activities.budget_plan` ตาม `budget_source_id` ของแต่ละกิจกรรม (SUM ... GROUP BY project, budget_source) เพื่อเติมคอลัมน์ pivot ต่อแถวโครงการ

### 11.2 รายงานที่ 2 — แผนปฏิบัติการรายละเอียดต่อโครงการ (ไฟล์ `แผนงานที่_1_การพัฒนาคุณภาพชีวิตคนไทยทุกกลุ่มวัย.xls`)
โครงสร้าง: 1 แผนงาน/1 โครงการหลัก ต่อ 1 ชีต (`PJ1`, `PJ2`, ...) หัวรายงานเดียวกับข้อ 11.1 ตามด้วยตารางรายกิจกรรม คอลัมน์:
กลวิธี/กิจกรรมหลัก · วัตถุประสงค์ · ตัวชี้วัดและค่าเป้าหมาย · พื้นที่เป้าหมาย · กลุ่มเป้าหมาย · ระยะเวลาดำเนินการ (ทำเครื่องหมาย x รายเดือน ต.ค.–ก.ย. แบ่ง 4 ไตรมาส) · งบประมาณแยกตามแหล่ง (NON UC / เงินบำรุง / อื่นๆ) + รวม · หน่วยงาน/ผู้รับผิดชอบ

**สิ่งที่ต้องเก็บเพิ่ม (ทั้งหมดย้ายไปอยู่ระดับ `activities` แล้วตามตารางข้อ 3):**
- `activities.objective`, `activities.kpi_target_text`, `activities.target_area`, `activities.responsible_person`
- `activity_months` (M:N) — ใช้ทำเครื่องหมาย x ในคอลัมน์เดือน แทนการเก็บเป็นช่วงวันที่อย่างเดียว
- `activities.budget_source_id` — ใช้กำหนดว่ากิจกรรมนี้ตกในคอลัมน์ NON UC / เงินบำรุง / อื่นๆ ของตาราง

### 11.3 แนวทาง implement (แนะนำ ไม่ใช่ข้อบังคับ)
- ใช้ไลบรารี PhpSpreadsheet สร้างไฟล์ .xlsx ตาม layout เดิม (merge cell, subtotal ต่อกลุ่ม) เพื่อให้หน้าตาใกล้เคียงไฟล์ที่หน่วยงานคุ้นเคยที่สุด
- ตัวกรองรายงานอย่างน้อย: ปีงบประมาณ (`fiscal_years`), เครือข่าย/หน่วยงาน, แผนงาน — เพื่อให้พิมพ์ทีละแผนงานได้เหมือนไฟล์ตัวอย่าง (1 แผนงาน/ไฟล์)
- Query หลักคือการ join `projects` → `activities` (group by budget_source) → `activity_months` ซึ่งทุกตารางออกแบบรองรับไว้แล้วในข้อ 3 และ 11.1–11.2

## 12. Login 3 ช่องทาง — Google, LINE, และ Username/Password

ปรับจากเดิม (ที่ตัด password ออกทั้งหมด) เป็น **เปิด 3 ช่องทางพร้อมกัน**: Google OAuth 2.0, LINE Login, และ **Username/Password แบบดั้งเดิม** — ช่องทางหลังสุด**สมัครสมาชิกได้เอง** (ผ่านการยืนยันอีเมล) หรือ admin จะสร้างบัญชีให้โดยตรงก็ได้เช่นกัน (เช่น บัญชีทดสอบ/บัญชีระบบ) — ผู้ใช้ใหม่ทุกช่องทางยัง**ต้องรออนุมัติจาก admin ก่อน**เหมือนเดิม (`users.status = Pending` → admin กด Approve)

### 12.1 การเปลี่ยนแปลงตาราง `users`
- `auth_provider` ENUM(`google`,`line`,`local`) — เพิ่มค่า `local` สำหรับช่องทาง username/password
- `provider_user_id` — ใช้เฉพาะ `google`/`line` (ค่า `sub` จาก Google หรือ `userId` จาก LINE, unique ร่วมกับ `auth_provider`) เป็น `NULL` สำหรับบัญชี `local`
- **`username`** (ใหม่) — ใช้เฉพาะบัญชี `local`, unique, ไม่ซ้ำกันทั้งระบบ, เป็น `NULL` สำหรับบัญชี Google/LINE
- **`password_hash`** (ใหม่ กลับมาใช้) — ใช้เฉพาะบัญชี `local` เก็บด้วย `password_hash()` ของ PHP (bcrypt) เป็น `NULL` สำหรับบัญชี Google/LINE
- `email`, `name`, `avatar_url` — บัญชี Google/LINE ดึงมาอัตโนมัติตอน login ครั้งแรก; บัญชี `local` ให้ admin กรอกตอนสร้าง (`email` ควรบังคับกรอกสำหรับบัญชี local ด้วย เพื่อใช้ตอนลืมรหัสผ่าน ดูข้อ 12.5)
- `phone` (ถ้าต้องการ) เก็บไว้เป็นข้อมูลติดต่อเสริมเท่านั้น ไม่ใช่ช่องทาง login ไม่ว่าประเภทใด

**กติกาความสอดคล้อง (constraint ระดับ business logic ที่ต้องเช็คตอนบันทึก):** ถ้า `auth_provider = 'local'` ต้องมี `username` และ `password_hash` ครบ (provider_user_id เป็น NULL); ถ้า `auth_provider` เป็น `google`/`line` ต้องมี `provider_user_id` (username/password_hash เป็น NULL) — ป้องกันข้อมูลกำกวมว่าบัญชีนี้ควร login ทางไหน

### 12.2 หน้า Login แสดงทั้ง 3 ช่องทาง
หน้า `login.php` มีทั้งปุ่ม OAuth และฟอร์มกรอกรหัสผ่านในหน้าเดียวกัน:
1. ปุ่ม **"เข้าสู่ระบบด้วย Google"**
2. ปุ่ม **"เข้าสู่ระบบด้วย LINE"**
3. เส้นคั่น "หรือ" ตามด้วยฟอร์ม **Username / Password** พร้อมปุ่ม "เข้าสู่ระบบ" และลิงก์ **"ลืมรหัสผ่าน?"**

### 12.3 ขั้นตอนการ login แยกตามช่องทาง
**Google/LINE (OAuth):**
1. ผู้ใช้กดปุ่ม → ถูก redirect ไปหน้า consent ของ Google/LINE → ยืนยันตัวตนสำเร็จ → provider ส่ง authorization code กลับมาที่ `oauth_callback.php`
2. ระบบแลก code เป็น access token แล้วดึงโปรไฟล์ (email, ชื่อ, รูป, provider_user_id)
3. ค้นหาใน `users` ด้วย (`auth_provider`, `provider_user_id`):
   - **พบแล้ว + status = Approve** → สร้าง session, เข้าใช้งานได้ทันที
   - **พบแล้ว + status = Pending** → แจ้ง "บัญชีนี้รอการอนุมัติจากผู้ดูแลระบบ" ไม่สร้าง session
   - **ไม่พบ (login ครั้งแรก)** → สร้างแถวใหม่ใน `users` ด้วย `status = Pending`, `level` เว้นว่าง/ค่าเริ่มต้นต่ำสุด → แจ้งผู้ใช้ว่าอยู่ระหว่างรออนุมัติ **พร้อมแจ้งเตือน admin ทางอีเมล** (ใช้กลไกเดียวกับข้อ 6.1)

**Username/Password (local):**
1. ผู้ใช้กรอก username + password → ระบบค้นหาแถวที่ `auth_provider = 'local'` และ `username` ตรงกัน
2. เช็ค `password_verify()` กับ `password_hash` ที่เก็บไว้
3. รหัสผ่านถูกต้อง + `status = Approve` + อีเมลยืนยันแล้ว → สร้าง session เข้าใช้งานได้; รหัสผ่านผิด → ข้อความ "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" (ข้อความรวม ไม่บอกว่าผิดจุดไหนเพื่อความปลอดภัย)
4. **สมัครได้เอง** ผ่านลิงก์ "สมัครสมาชิก" บนหน้า login — ดูขั้นตอนเต็มในข้อ 12.4

### 12.4 การสมัครสมาชิกด้วยตนเอง (Self-Registration) — ช่องทาง Username/Password
1. ผู้ใช้กดลิงก์ **"สมัครสมาชิก"** จากหน้า login → ไปหน้า `register.php` กรอก: username, password + ยืนยันรหัสผ่าน, ชื่อ-สกุล, **email (บังคับ)**
2. ตรวจสอบตอนกรอก (real-time, บล็อกจริงไม่ใช่แค่เตือน เพราะ username ต้องไม่ซ้ำเพื่อใช้ login ได้ถูกต้อง):
   - `username` ต้องไม่ซ้ำกับที่มีอยู่แล้วในระบบ (เช็คทันทีที่พิมพ์เสร็จ ก่อนกดบันทึกด้วยซ้ำ)
   - รหัสผ่านตรงตามนโยบาย (ข้อ 12.5) และ "ยืนยันรหัสผ่าน" ต้องตรงกับ "รหัสผ่าน"
   - `email` ต้องเป็นรูปแบบอีเมลที่ถูกต้อง และไม่ซ้ำกับบัญชีอื่นที่มีอยู่แล้ว
3. บันทึกบัญชีใหม่ทันทีด้วย `status = Pending`, `auth_provider = 'local'`, `email_verified_at = NULL`
4. **ต้องยืนยันอีเมลก่อน** — ระบบส่งอีเมลพร้อมลิงก์ยืนยัน (token หมดอายุ 24 ชม. คนละชุดกับ `password_resets`) ไปที่อีเมลที่กรอก ผู้ใช้ต้องกดลิงก์ยืนยันก่อน จึงจะถือว่าอีเมลใช้งานได้จริง (`email_verified_at` ถูกตั้งค่า) — ป้องกันกรอกอีเมลปลอม/อีเมลคนอื่นตอนสมัคร และทำให้ฟีเจอร์ลืมรหัสผ่าน (ข้อ 12.5) ใช้งานได้จริงในอนาคต
5. เมื่อยืนยันอีเมลแล้ว บัญชียังคงเป็น `status = Pending` เหมือนช่องทาง OAuth — **แจ้งเตือน admin ทางอีเมลว่ามีผู้ใช้ใหม่รออนุมัติ** (ใช้กลไกเดียวกับข้อ 6.1) และ admin ต้อง**เลือก `level` และ `office_id`** ให้ก่อนกด Approve เหมือนบัญชี OAuth (แบบฟอร์มสมัครไม่มีช่องนี้ เพื่อไม่ให้ผู้สมัครตั้งสิทธิ์ตัวเองได้)
6. **ป้องกันการสมัครสแปม/บอทอัตโนมัติ:** เพิ่ม CAPTCHA อย่างง่าย (เช่น Google reCAPTCHA v2/v3 หรือ honeypot field ที่ซ่อนไว้ให้บอทกรอกแต่คนจริงมองไม่เห็น) บนฟอร์มสมัครสมาชิก เพราะเป็นหน้าเปิดสาธารณะที่ไม่มีการยืนยันตัวตนจากภายนอกเหมือน OAuth

### 12.5 การจัดการบัญชี Username/Password โดย admin
- นอกจากรออนุมัติบัญชีที่สมัครเอง (ข้อ 12.4) หน้า `users/add.php` (admin) ยังคงสร้างบัญชี `local` ให้ผู้ใช้โดยตรงได้เช่นกัน (เช่น กรณีเจ้าหน้าที่ไม่สะดวกสมัครเอง) — กรณีนี้ admin ตั้ง username + รหัสผ่านชั่วคราว → ระบบตั้ง flag **`must_change_password = 1`** บังคับให้ผู้ใช้เปลี่ยนรหัสผ่านทันทีหลัง login ครั้งแรก (ป้องกันรหัสผ่านชั่วคราวรั่วไหล) และ `email_verified_at` ตั้งให้ทันทีเพราะ admin เป็นผู้ยืนยันตัวตนแทน
- เพิ่มหน้า **"ลืมรหัสผ่าน"** (`forgot_password.php`): ผู้ใช้กรอก username/email → ระบบส่งลิงก์รีเซ็ตรหัสผ่านไปที่อีเมลที่ผูกไว้ (ใช้ token แบบสุ่มที่หมดอายุใน 30 นาที เก็บใน `password_resets` ตารางใหม่: `user_id`, `token`, `expires_at`) — ใช้กลไกอีเมลเดียวกับข้อ 6.1
- นโยบายรหัสผ่านขั้นต่ำ: ยาวอย่างน้อย 8 ตัวอักษร ผสมตัวอักษร+ตัวเลข — เช็คทั้ง client (คำแนะนำ) และ server (บังคับจริง)
- จำกัดจำนวนครั้งที่ login ผิดติดต่อกัน (เช่น 5 ครั้ง) แล้วล็อกบัญชีชั่วคราว 15 นาที กันการเดารหัสผ่าน (brute-force) — เพิ่มคอลัมน์ `failed_login_count`, `locked_until` ใน `users`

### 12.6 สิ่งที่ต้องเตรียมนอกระบบ (เฉพาะช่องทาง OAuth)
| Provider | ต้องสร้าง | ใช้ค่าอะไรในระบบ |
|---|---|---|
| Google | โปรเจกต์ใน Google Cloud Console → เปิด OAuth consent screen → สร้าง OAuth Client ID (Web application) | `Client ID`, `Client Secret`, `Redirect URI` (เช่น `https://โดเมน/oauth_callback.php?provider=google`) |
| LINE | ช่องทางใน LINE Developers Console → สร้าง "LINE Login" channel | `Channel ID`, `Channel Secret`, `Callback URL` เดียวกันแนวคิด |

เก็บค่าเหล่านี้ใน `config/oauth.php` (ไม่ commit ขึ้น git — ใช้ `.env` หรือไฟล์ config แยกนอก repo) ช่องทาง `local` ไม่ต้องเตรียมอะไรเพิ่มเพราะทำงานอยู่ภายในระบบทั้งหมด

### 12.7 ข้อควรระวัง
- ต้องใช้ HTTPS เสมอทั้ง 3 ช่องทาง (OAuth บังคับอยู่แล้ว ส่วน local ก็ต้อง HTTPS เพื่อไม่ให้รหัสผ่านวิ่งผ่านเครือข่ายแบบไม่เข้ารหัส)
- อีเมลจาก Google เชื่อถือได้ (verified) แต่ LINE **ไม่บังคับผูกอีเมล** — บางบัญชี LINE อาจไม่มีอีเมลส่งกลับมาเลย จึงต้องออกแบบให้ `email` เป็น nullable สำหรับบัญชี Google/LINE และใช้ `provider_user_id` เป็นตัวระบุตัวตนหลักของช่องทางนั้นแทน ไม่ใช่ email — ส่วนบัญชี `local` ต้องบังคับมี `email` เสมอเพราะจำเป็นสำหรับฟีเจอร์ลืมรหัสผ่าน
- การ "ปิดสิทธิ์ผู้ใช้" ทำผ่านการเปลี่ยน `status` เป็นค่าอื่นที่ไม่ใช่ Approve (เช่น `Suspended`) เหมือนเดิมสำหรับทุกช่องทาง — สำหรับบัญชี `local` เพิ่มอีกทางคือ admin กดปุ่ม "รีเซ็ตรหัสผ่าน" ให้ผู้ใช้ได้โดยตรงถ้าจำเป็น

## 13. มาตรฐานวันที่: ปฏิทินไทย พ.ศ. 4 หลัก (บังคับใช้ทั้งระบบ)

**หลักการสำคัญ:** เก็บวันที่ใน **MySQL เป็นค.ศ. (Gregorian) มาตรฐานเสมอ** (`DATE`/`DATETIME`) เพื่อให้คำนวณ/เปรียบเทียบ/เรียงลำดับวันที่ทำงานถูกต้องตามปกติ **ห้ามเก็บ พ.ศ. ลงฐานข้อมูลโดยตรง** — การแปลงเป็น พ.ศ. ทำที่ "ชั้นแสดงผล" (presentation layer) เท่านั้น

| ส่วน | แนวทาง |
|---|---|
| แสดงผล (ทุกหน้าจอ, ทุกรายงาน, ทุก PDF/Excel) | แปลง ค.ศ. → พ.ศ. ด้วยการ **+543** แสดงเป็น 4 หลักเสมอ (เช่น 2025 → **2568**, ห้ามแสดง 2 หลักแบบ "68") รูปแบบมาตรฐาน: `d MMMM yyyy` เช่น "10 กันยายน 2568" (แบบเต็ม) หรือ `dd/MM/yyyy` เช่น "10/09/2568" (แบบย่อ) โดยใช้ชื่อเดือนไทยเต็ม (มกราคม–ธันวาคม) ในแบบเต็ม |
| ฟังก์ชันกลาง (ฝั่ง PHP) | สร้าง helper กลางจุดเดียว เช่น `thai_date($date, $format='long')` ใช้ทุกหน้าแทนการเขียน logic แปลงซ้ำในแต่ละไฟล์ — ป้องกันความไม่สม่ำเสมอ |
| ฟอร์มกรอกวันที่ (input) | `<input type="date">` ของ HTML5 แสดงปี ค.ศ. ตาม locale เบราว์เซอร์เสมอ **บังคับ พ.ศ. ไม่ได้ตรงๆ** จึงต้องใช้ **Thai datepicker แบบ custom** (เช่น flatpickr ที่ตั้งค่า locale ไทย + ปรับปี +543 เอง หรือเขียน dropdown วัน/เดือน/ปี-พ.ศ. เอง) ผู้ใช้เห็นเฉพาะ พ.ศ. ตลอดเวลา แล้ว JS แปลงกลับเป็น ค.ศ. ก่อนส่งค่าไป backend |
| ปีงบประมาณ (`fiscal_years.year_be`) | เก็บเป็นเลข พ.ศ. ตรง ๆ ได้เลย (ไม่ต้องแปลง) เพราะเป็นแนวคิดที่ใช้ พ.ศ. อยู่แล้วในราชการไทย (เช่น "ปีงบประมาณ 2569") — แต่ `start_date`/`end_date` ของปีนั้นยังเก็บเป็น ค.ศ. ปกติ (เช่น 2569 → start 1 ต.ค. 2025, end 30 ก.ย. 2026) |
| Timestamp ระบบ (created_at, updated_at, log ต่าง ๆ) | เก็บเป็น ค.ศ./DATETIME ปกติของ MySQL เสมอ แสดงผลค่อยแปลงเป็น พ.ศ. ตอน render เท่านั้น |

## 14. มาตรฐานข้อความแจ้งเตือนและสถานะ "กำลังโหลด" — ต้องเป็นภาษาไทยที่เข้าใจง่าย

ทุกจุดที่ระบบสื่อสารกับผู้ใช้ (ข้อความแจ้งเตือน, ปุ่มระหว่างทำงาน, สถานะโหลดข้อมูล) **ต้องเป็นภาษาไทยล้วน ไม่ใช้ศัพท์เทคนิคหรือภาษาอังกฤษปนโดยไม่จำเป็น** ใช้ประโยคสั้น กระชับ บอกสิ่งที่เกิดขึ้นจริงและสิ่งที่ผู้ใช้ควรทำต่อ

### 14.1 ข้อความแจ้งเตือน (Flash / Toast) — ชุดคำมาตรฐาน
| สถานการณ์ | ข้อความตัวอย่าง |
|---|---|
| บันทึก/เพิ่มสำเร็จ | "บันทึกข้อมูลเรียบร้อยแล้ว" |
| แก้ไขสำเร็จ | "แก้ไขข้อมูลเรียบร้อยแล้ว" |
| ลบสำเร็จ | "ลบข้อมูลเรียบร้อยแล้ว" |
| เกิดข้อผิดพลาดทั่วไป | "เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง" (ไม่โชว์ error code/SQL message ให้ผู้ใช้ทั่วไปเห็น) |
| ไม่มีสิทธิ์เข้าถึง | "คุณไม่มีสิทธิ์เข้าถึงหน้านี้" |
| ยืนยันก่อนลบ | "ยืนยันการลบรายการนี้ใช่หรือไม่? การลบไม่สามารถย้อนกลับได้" |
| รออนุมัติบัญชี | "บัญชีของท่านอยู่ระหว่างรอการอนุมัติจากผู้ดูแลระบบ" |
| ฟอร์มกรอกไม่ครบ | ระบุชัดว่าช่องไหนขาด เช่น "กรุณากรอก \"ชื่อโครงการ\" ก่อนบันทึก" (ไม่ใช้ "invalid input" ลอย ๆ) |

### 14.2 สถานะ "กำลังโหลด" / "กำลังบันทึก"
| จุดที่ต้องมี loading indicator | ข้อความที่ใช้ |
|---|---|
| โหลดกราฟ Dashboard (fetch JSON) | สปินเนอร์ + "กำลังโหลดข้อมูล..." |
| Cascading dropdown (เลือกแผนงาน → โหลดโครงการหลัก/KPI ข้อ 10.1) | dropdown ปลายทางแสดง option เดียวชั่วคราวว่า "กำลังโหลด..." แล้วเปลี่ยนเป็นตัวเลือกจริงเมื่อโหลดเสร็จ |
| กดปุ่มบันทึกฟอร์ม | ปุ่มเปลี่ยนข้อความเป็น "กำลังบันทึก..." และ disable ปุ่มทันทีเพื่อกันกดซ้ำ (double-submit) |
| Export รายงาน (ข้อ 11 — ไฟล์ใหญ่อาจใช้เวลา) | "กำลังสร้างรายงาน กรุณารอสักครู่..." พร้อม progress bar หรือสปินเนอร์ |
| โหลดหน้าที่มีข้อมูลเยอะ (ตาราง DataTables) | ใช้ข้อความ loading ของ DataTables ภาษาไทยที่ตั้งไว้แล้ว ("กำลังโหลด...") |

### 14.3 หลักการเขียนข้อความ (Writing guideline)
- ใช้ประโยคบอกเล่า ไม่กำกวม เช่น "บันทึกไม่สำเร็จ เนื่องจากงบประมาณติดลบ" ดีกว่า "Error: budget < 0"
- คำลงท้ายสุภาพสม่ำเสมอทั้งระบบ (เช่น ใช้ "กรุณา" ไม่ใช่ "โปรด" ปนกัน) — กำหนดเป็น glossary กลางไว้ใน `includes/messages.php` เพื่อไม่ให้แต่ละโมดูลเขียนคำไม่ตรงกัน
- ข้อความ error ทางเทคนิค (SQL exception, stack trace) **ห้ามแสดงให้ผู้ใช้เห็นเด็ดขาด** — log เก็บไว้ฝั่ง server เท่านั้น ผู้ใช้เห็นแค่ข้อความสุภาพว่าเกิดข้อผิดพลาด

## 15. ระบบติดตามการแก้ไขข้อมูล (Audit Trail) — ใครแก้ / แก้อะไร / แก้ตรงไหน

แยกออกเป็น 2 ตารางเพื่อตอบ 3 คำถามนี้ได้ครบ: **ใคร** (changed_by) **แก้ตรงไหน** (table_name + record_id) **แก้อะไร** (audit_log_details ระบุเป็นรายฟิลด์)

### 15.1 โครงสร้างตาราง

**`audit_log`** (หัวรายการ 1 แถว = การบันทึก 1 ครั้ง)
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| table_name | ชื่อตารางที่ถูกแก้ เช่น `projects`, `activities`, `budget_disbursements` |
| record_id | id ของแถวที่ถูกแก้ในตารางนั้น |
| action | `create` / `update` / `delete` |
| changed_by | FK `users.id` (ใคร) |
| changed_at | วันเวลาที่แก้ (ค.ศ. ในฐานข้อมูล → แสดงผล พ.ศ. ตามข้อ 13) |
| ip_address | IP ผู้แก้ไข (เสริมความน่าเชื่อถือของหลักฐาน) |

**`audit_log_details`** (รายละเอียดระดับฟิลด์ — 1 แถวต่อ 1 ฟิลด์ที่เปลี่ยน)
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| audit_log_id | FK ไปหัวรายการด้านบน |
| field_name | ชื่อคอลัมน์จริงในฐานข้อมูล เช่น `sub_project_name`, `budget_approved`, `status_id` |
| old_value | ค่าก่อนแก้ (เก็บเป็น TEXT ทุกชนิดข้อมูล แปลงเป็น string ตอนบันทึก) |
| new_value | ค่าหลังแก้ |

**ทำไมแยก 2 ตาราง แทน JSON เดียว:** เพราะการแยกฟิลด์ทำให้ค้นหา/แสดงผล "เฉพาะรายการที่แก้ไขงบประมาณ" หรือ "เฉพาะรายการที่แก้ไขสถานะ" ได้ง่ายด้วย SQL ธรรมดา (`WHERE field_name = 'budget_approved'`) และแสดงเป็นประโยคภาษาไทยอ่านง่ายได้ทันที เช่น "เปลี่ยนงบประมาณจาก 44,780 เป็น 50,000 บาท" โดยไม่ต้อง parse JSON

**ตารางที่ต้องเก็บ audit (ขอบเขต):** `projects`, `activities`, `budget_disbursements`, `project_kpi`, `kpi_progress`, `objectives`, `key_results`, `project_materials`, `project_priority_levels`, `project_facilities`, และ**ตารางข้อมูลหลักทั้ง 12 ตารางในข้อ 10/19/23** (เพราะ admin แก้ค่ากลางเหล่านี้กระทบทั้งระบบ ต้องตรวจสอบย้อนหลังได้เช่นกัน)

**ข้อสำคัญ — ต้อง append-only:** ห้ามมีการ UPDATE/DELETE แถวใน `audit_log`/`audit_log_details` เด็ดขาด (ไม่มีปุ่มลบในหน้าจอใด ๆ) และเวลาลบข้อมูลต้นทางจริง (เช่นลบโครงการ) **ห้ามตั้ง FK แบบ CASCADE ลบ audit_log ตามไปด้วย** — เก็บ `record_id` เป็นเลขลอยไม่ผูก FK บังคับ เพื่อให้ประวัติยังอยู่แม้ข้อมูลต้นทางถูกลบไปแล้ว

### 15.2 วิธีบันทึก (แนวคิดระดับออกแบบ ไม่ใช่โค้ด)
ทุกจุดที่มีการ INSERT/UPDATE/DELETE ตารางในขอบเขตข้างต้น ให้เรียกใช้ฟังก์ชันกลางร่วมกัน (เช่น `logAudit($table, $id, $action, $oldRow, $newRow)`) ที่ทำหน้าที่:
1. เทียบค่าฟิลด์เก่ากับใหม่ทีละคอลัมน์ (ข้ามคอลัมน์ที่ไม่เปลี่ยน)
2. เขียน 1 แถวใน `audit_log` (หัวรายการ) + 1 แถวต่อฟิลด์ที่เปลี่ยนใน `audit_log_details`
3. ดึง `changed_by` จาก session ผู้ใช้ปัจจุบันเสมอ ไม่ต้องส่งพารามิเตอร์แยก

วางฟังก์ชันนี้ไว้ที่ `includes/audit.php` เรียกจากทุกโมดูล (`projects/`, `activities/`, `budget/`, `kpi/`, และหน้าจัดการข้อมูลหลักในข้อ 10) เพื่อไม่ให้พลาดจุดใดจุดหนึ่ง

### 15.3 การแสดงผล — หน้า "ประวัติการแก้ไข"
- **ระดับโครงการ:** เพิ่ม Tab ใหม่ "ประวัติการแก้ไข" ในหน้ารายละเอียดโครงการ (ข้อ 4) แสดง timeline รวมของ `audit_log` (ทุกฟิลด์ที่เคยแก้ในโครงการนั้นและกิจกรรม/งบ/KPI ที่อยู่ใต้โครงการนั้น) เรียงเวลาล่าสุดก่อน รูปแบบ: **[วันที่ พ.ศ.] [ชื่อผู้แก้ไข] แก้ไข [ชื่อฟิลด์ภาษาไทย] จาก "[ค่าเก่า]" เป็น "[ค่าใหม่]"**
- **ระดับข้อมูลหลัก (admin):** แต่ละหน้าจัดการข้อมูลหลักในข้อ 10 มีปุ่ม "ดูประวัติการแก้ไข" ต่อรายการ
- **ชื่อฟิลด์ต้องแปลเป็นภาษาไทยเสมอ** ไม่แสดงชื่อคอลัมน์ดิบ เช่น `sub_project_name` → ให้ขึ้นว่า "ชื่อโครงการย่อย" — ทำ dictionary กลาง (array คงที่ table+field → label ไทย) ไว้ใน `includes/audit.php` ใช้ร่วมกับที่ใช้ตั้งชื่อ label ในฟอร์มอยู่แล้ว เพื่อไม่ต้องเขียนซ้ำ
- ค่าที่เป็น ID (เช่น `status_id`, `department_id`) ต้องแปลงเป็นชื่อที่อ่านได้ก่อนแสดง (join กับตาราง master ที่เกี่ยวข้อง) ไม่แสดงเลข id ดิบให้ผู้ใช้เห็น

### 15.4 ความสัมพันธ์กับ `project_tracking_log` ที่มีอยู่แล้ว
`project_tracking_log` (ข้อ 3–5) ยังคงไว้เหมือนเดิมสำหรับ **ประวัติ workflow การอนุมัติ/ตีกลับโดยเฉพาะ** (มี remark, pm_name ประกอบ) ส่วน `audit_log` เป็นการติดตาม **การแก้ไขข้อมูลฟิลด์ทั่วไปทุกชนิด** (รวมถึงกรณีแก้ `status_id` ตรงๆ ผ่านฟอร์มแก้ไขโดยไม่ผ่านปุ่มอนุมัติ/ตีกลับ) — หน้า Tab "ประวัติการแก้ไข" ในข้อ 15.3 ควรรวมทั้งสองแหล่งมาเรียงตามเวลาเดียวกันให้ผู้ใช้เห็นภาพครบในที่เดียว แต่ยังคง query แยกตารางกันตามเดิม (ไม่ต้องรวมเป็นตารางเดียว)

## 16. ปุ่มคำอธิบายการกรอกข้อมูล (Inline Help) — ทุกฟอร์มในระบบ

ทุกช่องกรอกข้อมูลที่อาจทำให้ผู้ใช้สับสน (โดยเฉพาะฟิลด์ที่คำนวณอัตโนมัติ, ฟิลด์ที่ถูกกรองด้วยเงื่อนไข, หรือฟิลด์ที่มีความหมายเฉพาะทางราชการ) ต้องมี **ปุ่ม/ไอคอนคำอธิบาย (ⓘ)** วางไว้ข้างป้ายชื่อฟิลด์ (label) กดแล้วเห็นคำอธิบายทันทีโดยไม่ต้องออกจากฟอร์ม

### 16.1 รูปแบบ UI
- ใช้ไอคอนวงกลมตัว "i" (Bootstrap Icons `bi-info-circle`) ขนาดเล็กต่อท้าย label ทุกฟิลด์ที่มีคำอธิบาย
- กดแล้วเปิดเป็น **popover** (Bootstrap popover) ลอยข้าง ๆ ไอคอน ไม่ใช่ modal เต็มจอ — เพื่อให้ยังเห็นฟอร์มอยู่ระหว่างอ่านคำอธิบาย ปิดได้ด้วยการคลิกที่อื่นหรือกดไอคอนซ้ำ
- เนื้อหาเป็น**ข้อความ static ฝังในหน้าเว็บ** (ไม่ต้อง fetch เพิ่ม) จึงเปิดได้ทันทีไม่ต้องมี loading indicator
- ฟิลด์ที่เป็น**ค่าคำนวณอัตโนมัติ/แก้ไขตรงไม่ได้** (เช่น `projects.budget_approved`) ให้ใส่ไอคอนล็อก (`bi-lock`) คู่กับ (ⓘ) เพื่อให้เห็นชัดตั้งแต่แรกว่าทำไมช่องนี้กรอกไม่ได้ โดยไม่ต้องกดดูก่อนถึงจะรู้

### 16.2 แหล่งข้อมูลคำอธิบาย (ใช้ dictionary กลางร่วมกับข้อ 15)
ขยาย dictionary ที่ออกแบบไว้ในข้อ 15.3 (table+field → label ภาษาไทย) ให้มี**คำอธิบายยาว**เพิ่มอีก 1 ค่า ต่อ 1 ฟิลด์ (ไม่ใช่แค่ label สั้น ๆ) เก็บไว้ที่จุดเดียว เช่น `includes/field_help.php` โครงสร้างเป็น array `[table][field] = ['label' => 'ชื่อฟิลด์', 'help' => 'คำอธิบายยาว']` ใช้ร่วมกัน 3 จุด: (1) label ในฟอร์ม (2) popover คำอธิบาย (3) การแสดงผลประวัติแก้ไขในข้อ 15 — แก้ที่เดียว อัปเดตครบทุกจุด ไม่ต้องเขียนคำอธิบายซ้ำหลายที่

### 16.3 ตัวอย่างคำอธิบายที่ควรมี (ตามฟิลด์ที่ซับซ้อนในระบบนี้โดยเฉพาะ)
| ฟิลด์ | คำอธิบายตัวอย่าง (ภาษาไทยเข้าใจง่าย) |
|---|---|
| `projects.budget_approved` (แสดงในหน้าโครงการ) | "งบประมาณนี้คำนวณอัตโนมัติจากผลรวมงบของทุกกิจกรรมในโครงการนี้ หากต้องการแก้ไข ให้ไปแก้ที่งบประมาณของแต่ละกิจกรรมแทน" |
| `main_project_id` (ตอนเลือกโครงการหลัก) | "ต้องเลือก \"แผนงาน\" ก่อน ระบบจะกรองให้เหลือเฉพาะโครงการหลักที่อยู่ในแผนงานนั้น" |
| `kpi_id` ตอนผูก KPI กับโครงการ | "รายการนี้กรองมาจากโครงการหลักที่เลือกไว้ หากไม่เห็น KPI ที่ต้องการ เลือก \"KPI อื่น ๆ\" เพื่อดูทั้งหมด" |
| `activities.budget_source_id` | "แหล่งงบนี้จะถูกใช้จัดกลุ่มในรายงานสรุปงบประมาณ เป็น 3 กลุ่มหลัก: NON UC (สป./กรม), เงินบำรุง (PP/CUP), และอื่นๆ (กองทุนต่าง ๆ)" |
| `activity_months` (ตาราง x รายเดือน) | "ทำเครื่องหมายเดือนที่วางแผนจะดำเนินกิจกรรมนี้ ใช้แสดงในรายงานแผนปฏิบัติการรายไตรมาส" |
| `fiscal_year_id` | "ปีงบประมาณของราชการไทย เริ่ม 1 ตุลาคม ถึง 30 กันยายนปีถัดไป เช่น ปีงบประมาณ 2569 คือ ต.ค. 2568 – ก.ย. 2569" |
| ปุ่ม "อนุมัติ" / "ตีกลับ" บนหน้าสถานะ | "อนุมัติจะเลื่อนไปขั้นถัดไปของกระบวนการ ตีกลับจะส่งกลับไปให้แก้ไขที่ขั้นก่อนหน้า ทั้งสองปุ่มจะถูกบันทึกไว้ในประวัติเสมอ" |
| `project_type` = จัดซื้อวัสดุ | "เลือกประเภทนี้เมื่อโครงการมีวัตถุประสงค์หลักเพื่อจัดซื้อวัสดุ/ครุภัณฑ์ ระบบจะเปิด Tab \"รายการวัสดุ\" เพิ่มเติมให้อัตโนมัติ" |
| คอลัมน์ pivot ในรายงานสรุปงบ (ข้อ 11) | ใส่ (ⓘ) ที่หัวคอลัมน์ตารางรายงานด้วยเช่นกัน (ไม่ใช่แค่ในฟอร์ม) อธิบายที่มาของยอดตัวเลขแต่ละคอลัมน์ เช่น "คอลัมน์นี้รวมจากกิจกรรมทุกรายการที่เลือกแหล่งงบเป็น กองทุนตำบล" |

### 16.4 หลักการเขียนคำอธิบาย
- สั้น กระชับ 1–2 ประโยค อ่านจบในไม่กี่วินาที (ยึดหลักการเขียนภาษาไทยเดียวกับข้อ 14.3)
- ถ้าฟิลด์เกี่ยวข้องกับการคำนวณ **ต้องบอกสูตร/ที่มาเป็นคำพูดธรรมดา** ไม่ใช่สัญลักษณ์ทางคณิตศาสตร์ เช่น "รวมจาก..." "คำนวณจาก..." แทนการเขียนสูตร SUM()
- ถ้าฟิลด์ถูกกรองแบบ cascading (ข้อ 10.1) ต้องบอกเงื่อนไขว่าอะไรมีผลต่อสิ่งที่เห็น (เช่น "ต้องเลือก X ก่อน ถึงจะเห็นตัวเลือกใน Y")
- ไม่จำเป็นต้องมีคำอธิบายทุกฟิลด์ — ใส่เฉพาะฟิลด์ที่มีคนถามซ้ำบ่อย/ตีความผิดได้ง่าย ฟิลด์พื้นฐาน (เช่น "ชื่อโครงการย่อย") ไม่จำเป็นต้องมี

## 17. ข้อเสนอแนะเพิ่มเติมเพื่อความสมบูรณ์ (ยืนยันแล้วว่าจะทำ)

### 17.1 ช่องค้นหากลาง (Global Search)
เพิ่มช่องค้นหาถาวรบน navbar (ทุกหน้า) ค้นพร้อมกันได้ทั้ง **โครงการ, กิจกรรม, และ KPI** ในครั้งเดียว
- Endpoint เดียว เช่น `api/global_search.php?q=คำค้น` รัน query แยกตามตาราง (`projects.sub_project_name`/`project_code`, `activities.activity_name`, `kpi_master.name`) แล้วรวมผลลัพธ์เป็นกลุ่ม พร้อมลิงก์ไปหน้ารายละเอียด
- **ต้องกรองผลลัพธ์ตามสิทธิ์การมองเห็น (ข้อ 6) เหมือนหน้าอื่น** — หน่วยบริการค้นแล้วเห็นเฉพาะของหน่วยตนเอง ไม่ใช่ค้นเจอทุกอย่างในระบบ
- พิมพ์แล้วค้นแบบ debounce (รอ ~300ms หลังหยุดพิมพ์) แสดงผลเป็น dropdown ใต้ช่องค้นหา ระหว่างรอแสดง "กำลังค้นหา..." ตามมาตรฐานข้อ 14
- ตารางที่ค้นบ่อยควรมี FULLTEXT INDEX รองรับ (ดูข้อ 17.6)

### 17.2 เตือนชื่อโครงการซ้ำ/คล้ายกัน
ตอนกรอก "ชื่อโครงการย่อย" ในฟอร์มเพิ่ม/แก้ไขโครงการ (ข้อ 4) ให้ระบบเช็คความคล้ายกับชื่อโครงการที่มีอยู่แล้วแบบ **real-time ไม่บล็อกการกรอก**
- ขอบเขตการเทียบ: ค้นภายใน**ปีงบประมาณเดียวกัน** (`fiscal_year_id`) เป็นหลัก เพราะโครงการซ้ำข้ามปีเป็นเรื่องปกติ (ทำโครงการเดิมต่อเนื่องทุกปี) แต่ซ้ำในปีเดียวกันน่าสงสัยกว่า
- วิธีเทียบ: เริ่มจากง่ายที่สุดคือ MySQL `FULLTEXT ... IN NATURAL LANGUAGE MODE` เพื่อจัดอันดับความคล้ายของคำ (ไม่ต้องพึ่ง Levenshtein/อัลกอริทึมซับซ้อนซึ่งไม่มีในตัว MySQL) — ถ้าความคล้ายเกิน threshold ที่ตั้งไว้ ให้ขึ้นเตือน
- UI: ข้อความเตือนสีเหลือง (ไม่ใช่สีแดง เพราะไม่ใช่ error) ใต้ช่องชื่อโครงการ "พบโครงการที่ชื่อคล้ายกันในปีงบประมาณนี้: [รายชื่อ + ลิงก์เปิดดู] หากมั่นใจว่าไม่ซ้ำ สามารถบันทึกต่อได้" — เป็นคำแนะนำ ไม่ใช่การบล็อก

### 17.3 กฎตรวจสอบอัตโนมัติ (Business Rule Validation)
กำหนดกฎกลางที่ระบบเช็คก่อนบันทึก/เปลี่ยนสถานะ แบ่งเป็น **บล็อกจริง (hard stop)** กับ **เตือนแต่ยังบันทึกได้ (advisory)**:

| กฎ | ระดับ | เหตุผล |
|---|---|---|
| ห้ามเปลี่ยนสถานะโครงการเป็น "เสนอโครงการ" (`2st1`) ถ้ายังไม่กรอก `situation_analysis`/`situation_data_source` หรือยังไม่เลือก `priority_levels` อย่างน้อย 1 รายการ | บล็อก | เหตุผล/ที่มาของโครงการต้องมีก่อนเข้าสู่กระบวนการอนุมัติจริง (ดูข้อ 22.3) — บันทึกเป็นร่างไว้ก่อนได้โดยไม่ต้องกรอกครบ |
| ห้ามเปลี่ยนสถานะโครงการเป็น "อนุมัติโครงการ" ถ้ายังไม่มีกิจกรรมเลยแม้แต่รายการเดียว | บล็อก | อนุมัติโครงการที่ไม่มีแผนกิจกรรม/งบประมาณรองรับไม่มีความหมาย |
| ห้าม "ส่งขอโอนเงิน" (ระดับโครงการ) ถ้า `budget_approved` รวมเป็น 0 | บล็อก | ไม่มีอะไรให้โอน |
| ห้ามลบสถานะใน `project_status`/`activity_status` ที่มีโครงการ/กิจกรรมอ้างอิงอยู่ | บล็อก (มีอยู่แล้วจาก FK constraint เดิม, ย้ำไว้ในกฎนี้ด้วย) | ป้องกันข้อมูลกำพร้า |
| วันที่ในกิจกรรม (`activity_months` ที่เลือก) ไม่อยู่ในช่วง `fiscal_years.start_date`–`end_date` ของโครงการที่สังกัด | เตือน (advisory) | อาจตั้งใจข้ามปีจริง ๆ ก็ได้ (เผื่อโครงการต่อเนื่อง) จึงไม่บล็อก |
| ผลรวม `budget_plan` ของกิจกรรมสูงผิดปกติเทียบค่าเฉลี่ยของโครงการประเภทเดียวกัน | เตือน (advisory) | ช่วยจับการกรอกเลขผิด (พิมพ์เกินหลัก) โดยไม่ไปจำกัดงบจริง |
| กด "อนุมัติ" ทั้งที่ฟิลด์สำคัญว่าง (เช่น ไม่มีผู้รับผิดชอบ/ไม่มีงบประมาณ) | เตือนก่อนยืนยัน (confirm dialog ภาษาไทย) | กันการอนุมัติข้อมูลไม่ครบโดยไม่ตั้งใจ |

วางไว้เป็นฟังก์ชันกลางร่วมกัน (เช่น `includes/validation.php`) เพื่อให้ทุกจุดที่เปลี่ยนสถานะ/บันทึกงบประมาณเรียกใช้ชุดเดียวกัน ไม่กระจายเขียนซ้ำในแต่ละไฟล์

### 17.4 มุมมอง "ภาพรวม KPI ทั้งจังหวัด"
เพิ่มหน้าใหม่ (เมนู KPI → Tab "ภาพรวมทั้งจังหวัด" แยกจากหน้า KPI รายโครงการเดิม) แสดง**ทุกโครงการที่ใช้ตัวชี้วัดเดียวกัน**มารวมไว้ในที่เดียว ต่อ 1 แถวของ **KPI หลัก** (ตอนนี้ `kpi_master` มีแต่ KPI หลักอยู่แล้วตามข้อ 18 จึงไม่ต้องกรองชั้นซ้อนเหมือนก่อน):
- จำนวนโครงการที่ผูก KPI หลักนี้ (นับจาก `project_kpi` แบบต่อเนื่อง) **และ** จำนวน Objective (จากทุกรอบ/ทุกโครงการ) ที่เคยตั้งเพื่อผลักดัน KPI หลักนี้ (ดูข้อ 18.5)
- ตารางเปรียบเทียบค่าต่อเนื่อง: โครงการ / ค่าเป้าหมาย (`project_kpi`) / ค่าจริงล่าสุด (`kpi_progress`) / สถานะ (สีเขียว = ถึงเป้า, สีแดง = ยังไม่ถึง) — **ไม่รวมตัวเลขเป็นก้อนเดียว** เพราะหน่วยวัด (`target_unit`) อาจต่างกันในแต่ละโครงการ (ร้อยละ vs จำนวนคน) การบวกรวมกันจะให้ตัวเลขที่ไม่มีความหมาย จึงแสดงเป็นตารางเทียบรายโครงการแทน พร้อมสรุปด้านบนว่า "กี่โครงการถึงเป้าแล้วจากทั้งหมด"
- คลิกดูรายละเอียดต่อ → เห็นรายการ Objective ทุกรอบที่เคยผูกกับ KPI หลักนี้ พร้อมคะแนนที่ปิดรอบแล้ว (รายละเอียดเต็มในข้อ 18.5)
- ตัวกรอง: ปีงบประมาณ, แผนงาน, กลุ่มงาน

### 17.5 พิมพ์หนังสือราชการอัตโนมัติ
เพิ่มปุ่ม **"พิมพ์หนังสือ"** บนหน้ารายละเอียดโครงการ แสดงเฉพาะประเภทเอกสารที่สอดคล้องกับสถานะปัจจุบัน (เช่น สถานะ "เสนอโครงการ" → ปุ่มพิมพ์ "บันทึกข้อความขออนุมัติโครงการ"; สถานะ "ส่งขอโอนเงิน" → ปุ่มพิมพ์ "หนังสือขอโอนเงิน")
- ทำเป็น**เทมเพลตเอกสาร** (ไฟล์ .docx ที่มีจุดใส่ placeholder เช่น `{{project_name}}`, `{{budget_total}}`, `{{responsible_person}}`) เก็บไว้ที่ `/templates/documents/` ระบบอ่านเทมเพลต แทนค่าจริงจากฐานข้อมูล แล้ว export เป็น .docx/PDF ให้ดาวน์โหลด/พิมพ์ทันที (ใช้ไลบรารี PhpWord)
- ทุกครั้งที่พิมพ์ ให้บันทึกสำเนาไฟล์ที่ generate ไว้ในตาราง `attachments` (ข้อ 3) โดยอัตโนมัติ เพื่อเป็นหลักฐานว่าพิมพ์ตอนไหน ข้อมูล ณ ขณะนั้นเป็นอย่างไร (เผื่อข้อมูลถูกแก้ไขภายหลัง ยังมีสำเนาต้นฉบับที่เคยพิมพ์ไว้)

### 17.6 Index ฐานข้อมูลเพื่อความเร็ว
กำหนด index หลักไว้ตั้งแต่ตอนสร้างตาราง (ไม่ต้องรอให้ช้าก่อนค่อยเพิ่ม):

| ตาราง | Index ที่ควรมี |
|---|---|
| `projects` | `UNIQUE(project_code)`, `INDEX(fiscal_year_id)`, `INDEX(owner_office_id)`, `INDEX(status_id)`, `FULLTEXT(sub_project_name)` (รองรับข้อ 17.1/17.2) |
| `activities` | `INDEX(project_id)`, `INDEX(status_id)`, `INDEX(budget_source_id)`, `FULLTEXT(activity_name)` |
| `budget_disbursements` | `INDEX(activity_id)`, `INDEX(project_id)`, `INDEX(disbursement_date)` |
| `project_tracking_log` / `activity_tracking_log` | `INDEX(project_id, tracking_date)` / `INDEX(activity_id, tracking_date)` |
| `kpi_master` | `FULLTEXT(name)` (รองรับข้อ 17.1), `INDEX(main_project_id)` |
| `audit_log` | `INDEX(table_name, record_id)`, `INDEX(changed_at)` |
| `users` | `UNIQUE(auth_provider, provider_user_id)` |

**ข้อควรระวัง:** อย่าใส่ index เกินความจำเป็นตั้งแต่แรก เพราะทุก index ที่เพิ่มทำให้ INSERT/UPDATE ช้าลงเล็กน้อย — ใส่เฉพาะคอลัมน์ที่ตารางนี้ระบุไว้ (ตรงกับ query หลักที่ออกแบบไว้ในเอกสารนี้) แล้วค่อยใช้ `EXPLAIN` ตรวจ query ที่ช้าจริงเมื่อข้อมูลเริ่มเยอะขึ้นในอนาคต ค่อยพิจารณาเพิ่มทีหลัง

## 18. ตัวชี้วัดย่อยที่ผู้ใช้กรอก ออกแบบเป็น OKR (Objectives and Key Results) — มีรอบเวลาและปิดรอบให้คะแนน

ปรับจากเดิม (ที่ให้พิมพ์ "KPI ย่อย" แบบไม่มีวันจบ) เป็น **โครงสร้าง OKR เต็มรูปแบบ**: ผู้ใช้ตั้ง **Objective** (เป้าหมายเชิงคุณภาพ มีกรอบเวลา) ที่ผูกกับ **KPI หลัก** เสมอ (ยังคงบังคับสัมพันธ์กับแผนงาน/โครงการหลักเหมือนเดิม) แล้วใส่ **Key Results** (ผลลัพธ์วัดผลได้) เป็นชุดย่อยของ Objective นั้น เมื่อครบกำหนดเวลาให้ **ปิดรอบพร้อมให้คะแนน** แล้วเริ่มรอบใหม่ — ต่างจาก KPI หลักที่วัดต่อเนื่องไม่มีวันจบ

### 18.1 โครงสร้างข้อมูล — เลิกใช้ `parent_kpi_id` แบบ self-reference เดิม

**`kpi_master`** กลับไปมีเฉพาะ **KPI หลัก** เท่านั้น (ตัดคอลัมน์ `parent_kpi_id`, `is_verified`, `created_by` ที่เคยเพิ่มไว้ออก เพราะแนวคิด "KPI ย่อยไม่มีวันจบ" ถูกแทนที่ด้วย OKR ทั้งหมดแล้ว) — ยังคงมี `main_project_id` ไว้เหมือนเดิมสำหรับ cascading (ข้อ 10.1)

**ตารางใหม่ — `objectives`** (1 แถว = 1 Objective ของ 1 โครงการ ใน 1 รอบเวลา):
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| project_id | FK `projects` |
| kpi_id | FK `kpi_master` — **บังคับเป็น KPI หลักที่สัมพันธ์กับแผนงาน/โครงการหลักของโครงการนี้เสมอ** (validation เดียวกับที่เคยออกแบบไว้ในข้อ 18.3 เดิม) |
| title | ข้อความ Objective เชิงคุณภาพ เช่น "ลดความเสี่ยงการเสียชีวิตของแม่และทารกแรกเกิดในพื้นที่รับผิดชอบ" |
| fiscal_year_id | FK `fiscal_years` |
| quarter | 1–4 (ไตรมาสที่เป็นกรอบเวลาของ Objective นี้) หรือ `NULL` ถ้าตั้งเป็นรายปีงบประมาณทั้งปี |
| status | ENUM(`active`,`closed`) |
| final_score | DECIMAL — คะแนนเฉลี่ยของ Key Results ทั้งหมด คำนวณตอนปิดรอบ (ข้อ 18.3), `NULL` ระหว่างที่ยัง active |
| created_by, created_at | ผู้สร้าง/เวลาที่สร้าง |

**ตารางใหม่ — `key_results`** (1 Objective มีได้หลาย Key Result):
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| objective_id | FK `objectives` |
| description | ข้อความ Key Result เช่น "หญิงตั้งครรภ์กลุ่มเสี่ยงสูงได้รับการติดตามครบ 4 ครั้งตามนัด เพิ่มจาก 60% เป็น 90%" |
| baseline_value | ค่าตั้งต้นก่อนเริ่มรอบ (เช่น 60) — ไม่บังคับ แต่ช่วยให้คำนวณคะแนนแม่นยำขึ้น |
| target_value | ค่าเป้าหมายปลายรอบ (เช่น 90) |
| target_direction | ENUM(`increase`,`decrease`) — **สำคัญ**: บาง Key Result ยิ่งมากยิ่งดี (เช่น อัตราติดตามครบนัด), บางอันยิ่งน้อยยิ่งดี (เช่น อัตราเสียชีวิต) ต้องระบุทิศทางเพื่อคำนวณคะแนนถูกต้อง |
| unit | หน่วยวัด เช่น "ร้อยละ", "คน", "ครั้ง" |
| final_score | DECIMAL 0.00–1.00 — คำนวณตอนปิดรอบ |
| created_by, created_at | |

**ตารางใหม่ — `key_result_progress`** (แทนที่ `kpi_progress` เดิมสำหรับ KPI ย่อย — บันทึกค่าจริงระหว่างรอบ):
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| key_result_id | FK `key_results` |
| record_date | วันที่บันทึก |
| actual_value | ค่าจริง ณ วันนั้น |
| note | หมายเหตุ |
| recorded_by | FK `users.id` |

### 18.2 ขั้นตอนในฟอร์ม (หน้าโครงการ → Tab KPI/OKR)
1. ผู้ใช้เลือก **"KPI หลัก"** ก่อนเสมอ จาก dropdown ที่กรองแบบ cascading ตามข้อ 10.1 (เฉพาะ KPI หลักที่ `main_project_id` ตรงกับโครงการที่เปิดอยู่) — เหมือนเดิมทุกประการ จุดนี้ไม่เปลี่ยน
2. กด **"+ ตั้ง Objective ใหม่"** ภายใต้ KPI หลักที่เลือก → กรอก title + เลือกปีงบประมาณ/ไตรมาส (ค่าเริ่มต้น = ไตรมาสปัจจุบันตาม `fiscal_years` ที่ active)
3. ภายใน Objective เดียวกัน กด **"+ เพิ่ม Key Result"** ได้หลายรายการ กรอก description, baseline, target, ทิศทาง (เพิ่ม/ลด), หน่วย
4. ระหว่างรอบ บันทึกความคืบหน้าของแต่ละ Key Result ผ่าน `key_result_progress` (เหมือนที่เคยบันทึก `kpi_progress` เดิม แต่ตอนนี้อยู่ใต้ Key Result แทน)
5. โครงการยังผูก KPI หลักตรง ๆ แบบไม่มี Objective ได้เหมือนเดิมถ้าไม่ต้องการตั้งรอบ OKR (ใช้ `project_kpi`/`kpi_progress` แบบเดิมคู่ขนานกันไป — สองระบบอยู่ร่วมกันได้ ไม่บังคับว่าทุก KPI หลักต้องมี Objective)

### 18.3 การปิดรอบและให้คะแนน (Scoring & Closing)
- เมื่อถึงวันสิ้นสุดไตรมาส/ปีงบประมาณของ Objective ระบบ**แจ้งเตือน**ผู้รับผิดชอบโครงการล่วงหน้า (ใช้กลไกเดียวกับข้อ 6.1) ว่า "Objective นี้ใกล้ครบกำหนด กรุณาปิดรอบ"
- ผู้ใช้กดปุ่ม **"ปิดรอบ"** (ไม่ปิดอัตโนมัติ ให้ผู้รับผิดชอบตรวจทานก่อนเสมอ) ระบบคำนวณคะแนนแต่ละ Key Result:
  - ทิศทาง `increase`: `score = LEAST((actual − baseline) / (target − baseline), 1.0)`
  - ทิศทาง `decrease`: `score = LEAST((baseline − actual) / (baseline − target), 1.0)`
  - ค่าจริงที่ใช้คำนวณ = ค่าล่าสุดใน `key_result_progress` ก่อนวันปิดรอบ
  - คะแนนไม่ติดลบ (ถ้าค่าจริงแย่กว่า baseline ให้ตัดที่ 0)
- `objectives.final_score` = ค่าเฉลี่ยของ `key_results.final_score` ทั้งหมดใน Objective นั้น
- หลังปิดรอบ (`status = closed`) **ห้ามแก้ไข/เพิ่ม progress ใน Key Result ของ Objective นั้นอีก** (ข้อมูลถูกแช่แข็งไว้เป็นประวัติ) — ต้องการรอบใหม่ต้องสร้าง Objective ใหม่เท่านั้น
- ปุ่ม **"เริ่มรอบใหม่จากอันนี้"** ที่ Objective ที่ปิดแล้ว ช่วย copy โครงสร้าง (title, KPI หลัก, รายการ Key Results) มาเป็นฉบับร่างของ Objective รอบถัดไป ให้แก้ target ใหม่แทนที่จะพิมพ์ใหม่ทั้งหมด

### 18.4 การแสดงผลประวัติ Objective ที่ปิดรอบแล้ว
หน้าโครงการ → Tab KPI/OKR แสดงเป็น timeline ของ Objective ทุกรอบ (active อยู่บนสุด, ปิดแล้วเรียงตามเวลาไล่ลง) แต่ละใบแสดง title, ช่วงเวลา (ไตรมาส/ปีงบประมาณ พ.ศ.), คะแนนรวม (ถ้าปิดแล้ว) พร้อม progress bar สีเขียว/เหลือง/แดงตามคะแนน (≥70% เขียว, 40–69% เหลือง, <40% แดง — เกณฑ์แนวทาง OKR ทั่วไปที่ Google ใช้)

### 18.5 ผลกระทบต่อรายงาน "ภาพรวม KPI ทั้งจังหวัด" (ข้อ 17.4)
หน้าภาพรวมยังคงจัดกลุ่มตาม **KPI หลัก** เป็นหลักเหมือนเดิม แต่เพิ่มมุมมองใหม่: คลิกดู KPI หลักแต่ละตัว → เห็นรายการ **Objective ทุกรอบจากทุกโครงการ**ที่เคยผูกกับ KPI หลักนั้น พร้อมคะแนนที่ปิดรอบแล้ว — ช่วยให้ผู้บริหารเห็นภาพว่าพื้นที่ไหนตั้ง Objective ผลักดัน KPI นี้บ้าง และทำได้ผลแค่ไหน (แยกจากตัวเลข KPI ต่อเนื่องที่ยังแสดงอยู่ในหน้าเดิม)

### 18.6 แหล่งอ้างอิงของ Key Result (Reference Links) — ยังคงมีเหมือนเดิม
`kpi_references` ยังคงอยู่ครบตามที่ออกแบบไว้เดิม เพียงเปลี่ยนจุดผูก **จาก `kpi_id` เป็น `key_result_id`** (FK `key_results.id`) เพราะ Key Result คือระดับที่ผู้ใช้กรอกเองแทนที่ "KPI ย่อย" เดิม:
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| key_result_id | FK `key_results.id` |
| url | ลิงก์แหล่งอ้างอิง (บังคับขึ้นต้นด้วย `http://` หรือ `https://`) |
| label | คำอธิบายสั้น ๆ ของลิงก์นี้ (เช่น "มติที่ประชุม พชอ. ครั้งที่ 3/2569") — ไม่บังคับ |
| added_by | FK `users.id` |
| added_at | วันเวลาที่เพิ่มลิงก์ |

พฤติกรรมการใช้งานเหมือนเดิมทุกประการ: เพิ่มได้หลายลิงก์ต่อ Key Result, ไอคอนลิงก์ (`bi-link-45deg`) เปิดแท็บใหม่เสมอ (`target="_blank" rel="noopener noreferrer"`), ตรวจแค่รูปแบบ URL ไม่เช็คว่าเข้าถึงได้จริง, เก็บ `added_by` ไว้ตรวจสอบย้อนหลังตามหลัก Audit Trail (ข้อ 15)

## 19. ระดับความสำคัญของโครงการ (Priority Levels) — ตั้งค่าได้เอง

เพิ่มการจัดหมวดหมู่โครงการตาม **ที่มา/ระดับความสำคัญเชิงนโยบาย** เช่น นโยบายระดับเขต, นโยบายระดับจังหวัด, ประเด็น พชอ. (คณะกรรมการพัฒนาคุณภาพชีวิตระดับอำเภอ), ปัญหาสุขภาพในพื้นที่ — **ตั้งค่ารายการเองได้** ไม่ hardcode ในโค้ด เพื่อให้เพิ่ม/แก้ชื่อ/ปิดใช้งานได้ตามนโยบายที่เปลี่ยนแปลงรายปี

### 19.1 ตารางที่เพิ่ม
- **`priority_levels`** (ตารางหลักใหม่ — ตัวที่ 11 ในกลุ่มข้อมูลหลักที่ admin จัดการได้ ข้อ 10): `id`, `code`, `name` (เช่น "นโยบายระดับเขต", "นโยบายระดับจังหวัด", "ประเด็น พชอ.", "ปัญหาสุขภาพในพื้นที่"), `sort_order`, `is_active`
- **`project_priority_levels`** (ตารางเชื่อม M:N ใหม่): `project_id`, `priority_level_id`, `note` (ช่องหมายเหตุสั้น ๆ อธิบายเพิ่มเติมว่าทำไมถึงเข้าข่ายระดับนั้น เช่น อ้างอิงเลขมติที่ประชุม พชอ.)

**ทำไมเป็น M:N ไม่ใช่ FK เดี่ยว:** โครงการหนึ่งอาจเข้าข่ายมากกว่า 1 ระดับพร้อมกันได้จริง (เช่น เป็นทั้งนโยบายระดับจังหวัด และตอบปัญหาสุขภาพในพื้นที่ไปพร้อมกัน) การบังคับเลือกได้ค่าเดียวจะไม่ตรงกับการใช้งานจริง

### 19.2 การแสดงผล
- ฟอร์มโครงการ (ข้อ 4): เพิ่มช่อง checkbox multi-select "ระดับความสำคัญ/ที่มาของโครงการ" ในส่วนข้อมูลทั่วไป
- หน้ารายการโครงการและหน้ารายละเอียด: แสดงเป็น badge สีต่อ 1 ระดับที่เลือกไว้ ต่อท้ายชื่อโครงการ
- เพิ่มเป็น**ตัวกรองใหม่**ในหน้ารายการโครงการและ Dashboard (ข้อ 1, 4) เช่น กรองดูเฉพาะโครงการที่เป็น "ประเด็น พชอ." ทั้งหมด — เป็นประโยชน์ตอนสรุปผลงานให้ผู้บริหารตามที่มานโยบาย
- อยู่ในขอบเขตของระบบ Audit Trail (ข้อ 15) เหมือนฟิลด์อื่น — ถ้ามีคนแก้ไขระดับความสำคัญของโครงการ จะถูกบันทึกไว้ในประวัติด้วย

## 20. บังคับกรอกข้อมูลทุกจุด — ยกเว้นงบประมาณกิจกรรมที่ไม่มี ให้ใส่ 0 แทนเว้นว่าง

**หลักการ:** ทุกฟิลด์ที่จำเป็นต่อการใช้งานจริง (NOT NULL ในฐานข้อมูล) ต้องบังคับกรอกก่อนบันทึกได้เสมอ ทั้งฝั่ง client (กันผู้ใช้กรอกไม่ครบ) และฝั่ง server (กันการข้าม validation ผ่าน request ตรง ๆ) — **ยกเว้นเรื่องงบประมาณของกิจกรรม**: บางกิจกรรมไม่มีค่าใช้จ่ายจริง (เช่น การประชุมหารือที่ไม่มีค่าใช้จ่าย) ในกรณีนี้ **ห้ามปล่อยว่าง (NULL) แต่ให้ใส่ค่า 0 แทน** เพื่อให้ผลรวมงบประมาณของโครงการ (`SUM(activities.budget_plan)`) คำนวณได้ถูกต้องเสมอโดยไม่ต้องเช็ค NULL แทรกในทุก query

### 20.1 กฎการบังคับกรอก (แบ่งตามชนิดฟิลด์)
| ชนิดฟิลด์ | กฎ |
|---|---|
| ข้อความ/ตัวเลือก ที่จำเป็น (เช่น ชื่อโครงการ, กลุ่มงาน, สถานะ) | บังคับกรอก/เลือก ห้ามว่าง |
| ตัวเลขงบประมาณ (`activities.budget_plan`) | **บังคับมีค่าเสมอ แต่อนุญาตเป็น 0** — ฟอร์มต้อง pre-fill ค่าเริ่มต้นเป็น `0` ไว้ในช่องตั้งแต่แรก (ไม่ใช่ช่องว่างเปล่า) ผู้ใช้แก้เป็นตัวเลขจริงถ้ามี หรือปล่อย 0 ไว้ถ้าไม่มีค่าใช้จ่าย — schema กำหนด `DEFAULT 0 NOT NULL` |
| ตัวเลขจำนวนเงินที่เป็น "ธุรกรรมจริง" (`budget_disbursements.amount`) | บังคับกรอก **และต้องมากกว่า 0** (ไม่อนุญาต 0 หรือค่าติดลบ) เพราะเป็นการบันทึกว่ามีการเบิกจ่ายเกิดขึ้นจริง การบันทึกรายการเบิกจ่าย 0 บาทไม่มีความหมาย |
| วันที่ที่จำเป็นต่อ workflow (เช่น `tracking_date`, `disbursement_date`) | บังคับกรอก ใช้ Thai datepicker เสมอ (ข้อ 13) |
| ฟิลด์เสริม/ไม่กระทบการทำงาน (เช่น หมายเหตุ, ลิงก์อ้างอิง) | ไม่บังคับ |
| ข้อความ "เหตุผล/ที่มา" ที่บังคับเฉพาะตอนเสนอโครงการ (`situation_analysis`, `situation_data_source`, `priority_levels` ≥1 รายการ) | **ไม่บล็อกตอนบันทึกร่าง แต่บล็อกตอนกดเปลี่ยนสถานะเป็น "เสนอโครงการ"** — ต่างจากฟิลด์บังคับทั่วไปในแถวบนสุดของตารางนี้ที่บล็อกตั้งแต่ตอนบันทึกครั้งแรก (ดูข้อ 22.3 และ 17.3) |

### 20.2 การแสดงผล
- ทุกช่องที่บังคับกรอก มีเครื่องหมาย **`*`** สีแดงต่อท้าย label เสมอ (ตามธรรมเนียมฟอร์มทั่วไป)
- ใช้ `required` ของ HTML5 เป็นด่านแรก แต่ **แทนที่ข้อความ error เริ่มต้นของเบราว์เซอร์ (ซึ่งเป็นภาษาอังกฤษ) ด้วยข้อความไทย** ผ่าน `oninvalid`/`setCustomValidity()` ให้ตรงกับชุดคำมาตรฐานในข้อ 14.1 เช่น "กรุณากรอก \"ชื่อโครงการย่อย\" ก่อนบันทึก"
- ช่องตัวเลขงบประมาณที่อนุญาต 0 ให้ใช้ `type="number" min="0" value="0" required` — ผู้ใช้ลบให้ว่างไม่ได้ (กลับไปเป็น 0 อัตโนมัติถ้าลบจนว่างแล้วออกจากช่อง) ป้องกันการส่งค่าว่างขึ้นไปที่ server
- ช่องจำนวนเงินธุรกรรมจริง (`amount`) ใช้ `type="number" min="0.01" required` ไม่ pre-fill 0 (เพราะต้องบังคับให้กรอกค่าจริงเอง ไม่ใช่ปล่อยผ่าน 0 ไปได้)

### 20.3 ฝั่ง Server (บังคับซ้ำเสมอ ไม่พึ่ง client อย่างเดียว)
ทุก endpoint ที่บันทึกข้อมูล (add/edit ของทุกโมดูล) ต้องตรวจสอบก่อน INSERT/UPDATE จริงว่า:
- ฟิลด์บังคับทุกตัวมีค่า (ไม่ใช่ string ว่าง, ไม่ใช่ NULL)
- ฟิลด์ตัวเลขงบประมาณกิจกรรม ถ้าไม่ได้ส่งมาหรือส่งค่าว่าง ให้ **ตั้งเป็น 0 อัตโนมัติที่ server** (เผื่อกรณี client ทำพลาดหรือปิด JavaScript) แทนที่จะปฏิเสธการบันทึก
- ฟิลด์ `amount` ของการเบิกจ่ายจริง ถ้า ≤ 0 ให้ **บล็อกการบันทึกจริง** พร้อมข้อความ "จำนวนเงินต้องมากกว่า 0 บาท"

หากพบฟิลด์บังคับขาดหาย ให้ตอบกลับด้วยข้อความ error ภาษาไทยตามมาตรฐานข้อ 14.1 ระบุชัดว่าช่องไหนขาด ไม่ใช่ข้อความรวม ๆ ว่า "invalid input"

## 21. ดีไซน์หน้า Dashboard ให้ดูทันสมัย เป็นมืออาชีพ (Visual Design System)

Bootstrap 5 แบบ default (สีน้ำเงิน `#0d6efd` มาตรฐาน, การ์ดขอบเหลี่ยม, เงาแบนราบ) จะดูเป็น "เว็บราชการทั่วไป" ทันที — ต้อง**ปรับธีมทับ Bootstrap** ด้วย custom CSS variables ไม่ใช้สีและ spacing ค่าเริ่มต้นตรง ๆ เพื่อให้ได้ลุค SaaS dashboard ทันสมัย (แนวเดียวกับ Notion, Linear, Vercel dashboard)

### 21.1 โทนสี (Color Palette)
| การใช้งาน | สี (ตัวอย่าง) | หมายเหตุ |
|---|---|---|
| Primary (ปุ่มหลัก, ลิงก์, accent) | Indigo `#4F46E5` → gradient ไปทาง Violet `#7C3AED` | ใช้แทนสีน้ำเงิน Bootstrap default ทั้งหมด |
| พื้นหลังหน้า | เทาอ่อนมาก `#F8F9FC` (ไม่ใช่ขาวล้วน ให้การ์ดขาวลอยเด่นขึ้น) | |
| การ์ด/พื้นผิว | ขาว `#FFFFFF` + เงานุ่ม `box-shadow: 0 2px 12px rgba(0,0,0,.06)` + ขอบมน `border-radius: 16px` | แทน `card-stat` เดิมที่ใช้ 14px/เงาเบา ให้เพิ่มความนุ่มขึ้นอีก |
| สถานะสำเร็จ/เตือน/ผิดพลาด | เขียว `#16A34A` / เหลืองอำพัน `#F59E0B` / แดง `#DC2626` | ใช้สม่ำเสมอทั้งระบบ (badge สถานะ, กราฟ, ข้อความแจ้งเตือนข้อ 14) |
| ตัวหนังสือหลัก/รอง | เทาเข้ม `#1E293B` / เทากลาง `#64748B` | เลี่ยงดำสนิท `#000` ให้ดูนุ่มตากว่า |

### 21.2 Typography
- ใช้ฟอนต์ไทยที่ดูทันสมัยแทน fallback ธรรมดาของเบราว์เซอร์ — แนะนำ **"Prompt"** หรือ **"IBM Plex Sans Thai"** จาก Google Fonts (รองรับภาษาไทยเต็มรูปแบบ น้ำหนักตัวอักษรหลากหลาย)
- Scale: หัวข้อใหญ่ (การ์ดสรุป/ตัวเลขเด่น) 28–32px ตัวหนา, หัวข้อรอง 18–20px, เนื้อหาปกติ 14–15px, ข้อความรอง/หมายเหตุ 12–13px สีเทากลาง
- ตัวเลขสำคัญ (งบประมาณ, จำนวนโครงการ) ใช้น้ำหนัก `font-weight: 700` ขนาดใหญ่กว่าป้ายกำกับชัดเจน เพื่อให้สแกนด้วยตาได้เร็ว

### 21.3 โครงสร้างหน้า Dashboard
- **เปลี่ยนจาก navbar บนอย่างเดียว เป็น Sidebar ซ้ายแบบ fixed** (สไตล์ dashboard บริษัทซอฟต์แวร์สมัยใหม่ทั่วไป): โลโก้/ชื่อระบบด้านบนสุด → เมนูหลักเรียงเป็นไอคอน+ข้อความ → โปรไฟล์ผู้ใช้ (รูปจาก Google/LINE หรือ initial ย่อชื่อ) ปักหมุดไว้ล่างสุดของ sidebar พร้อมเมนูออกจากระบบ
- **ส่วนหัว Dashboard**: คำทักทายตามช่วงเวลา เช่น "สวัสดีตอนเช้า, คุณสมชาย 👋" + วันที่ปัจจุบันแบบ พ.ศ. เต็ม (ข้อ 13) + ช่องค้นหากลาง (ข้อ 17.1) วางเด่นตรงกลางส่วนหัว
- **การ์ดสรุปสถิติ (stat cards)**: ออกแบบใหม่แต่ละใบมี ไอคอนอยู่ในกล่องพื้นหลัง gradient อ่อน (สีเดียวกับหมวดนั้น) + ตัวเลขใหญ่ตัวหนา + ป้ายกำกับเล็กใต้ตัวเลข + **ตัวชี้แนวโน้มเล็ก ๆ** (ลูกศรขึ้น/ลงพร้อม % เทียบเดือน/ปีก่อน สีเขียว/แดงตามทิศทาง) ให้ความรู้สึกเหมือนแดชบอร์ดธุรกิจจริง ไม่ใช่แค่กล่องตัวเลขเฉย ๆ
- **กราฟ**: ลดเส้น grid ให้บางจางหรือไม่มีเลย ใช้สีจาก palette เดียวกันทั้งชุด (ไม่ใช้สี default ของ Chart.js) เส้น/แท่งโค้งมนนุ่มนวล (`borderRadius` บนแท่ง bar chart)
- **การ์ด "กิจกรรมล่าสุด" (Recent Activity feed)**: รายการเหตุการณ์ล่าสุดในระบบ (โครงการเปลี่ยนสถานะ, มีการเบิกจ่ายใหม่) แสดงเป็น timeline แนวตั้งพร้อมไอคอนเล็ก ๆ ต่อประเภทเหตุการณ์ — เพิ่มความรู้สึก "มีชีวิต" ให้หน้าแรกไม่นิ่งเกินไป
- **Quick actions**: ปุ่มลัดเด่น ๆ (เพิ่มโครงการ, บันทึกเบิกจ่าย) จัดเป็นแถบด้านบนหรือมุมขวาบนของ Dashboard ใช้สี primary เด่นชัด

### 21.4 รายละเอียดปลีกย่อยที่ทำให้ดูมืออาชีพขึ้นมาก (Polish)
- **Hover elevation**: การ์ดยกตัวขึ้นเบา ๆ พร้อมเงาเข้มขึ้นเล็กน้อยเมื่อเอาเมาส์ชี้ (`transform: translateY(-2px)` + transition นุ่มนวล ~150ms) — ใช้กับการ์ดสรุปและการ์ดโครงการในหน้ารายการ
- **Skeleton loading แทน spinner ธรรมดา**: ระหว่างรอข้อมูล Dashboard โหลด (ข้อ 14.2) ให้แสดงกล่องสีเทาอ่อนเป็นโครงร่างของการ์ด/กราฟที่กำลังจะปรากฏ (shimmer effect) แทนสปินเนอร์กลม ๆ ลอย ๆ — ให้ความรู้สึกทันสมัยกว่าและลดความรู้สึก "รอนาน"
- **Empty state ที่ออกแบบแล้ว**: ถ้ายังไม่มีข้อมูล (เช่น โครงการยังไม่มีกิจกรรม) แสดงไอคอน/ภาพประกอบเรียบง่ายพร้อมข้อความชวนให้ทำขั้นตอนถัดไป (เช่น "ยังไม่มีกิจกรรม — เริ่มเพิ่มกิจกรรมแรกของโครงการนี้") แทนตารางว่างเปล่าห้วน ๆ
- Border radius สม่ำเสมอทั้งระบบ (การ์ด 16px, ปุ่ม/input 8–10px) และระยะห่าง (spacing) เป็นทวีคูณของ 4px (4/8/12/16/24/32) ให้จังหวะภาพรวมสม่ำเสมอ ไม่มั่ว

### 21.5 Responsive (มือถือ)
- จอเล็ก: sidebar ยุบซ่อน เปิดผ่านปุ่ม hamburger มุมซ้ายบน หรือเปลี่ยนเป็น bottom navigation bar แบบแอปมือถือสำหรับเมนูหลัก 4–5 อัน (Dashboard, โครงการ, กิจกรรม, งบประมาณ, เพิ่มเติม)
- การ์ดสรุปสถิติเรียงเป็นคอลัมน์เดียวจาก 4 คอลัมน์ (สำคัญเพราะผู้ใช้ภาคสนาม เช่น รพ.สต./อสม. มักเข้าผ่านมือถือ)

### 21.6 แนวทาง implement (ยังอยู่บนฐาน Bootstrap 5 เดิม ไม่ต้องเปลี่ยน framework)
- Override ผ่าน CSS variables ของ Bootstrap 5 (`--bs-primary`, `--bs-border-radius` ฯลฯ) ใน `assets/css/style.css` แทนการเขียน class ชนกับ Bootstrap เอง
- Import ฟอนต์จาก Google Fonts (`<link>` ใน header) — เลือก "Prompt" หรือ "IBM Plex Sans Thai" ตามข้อ 21.2
- ไอคอน: ใช้ Bootstrap Icons ต่อไปได้ (ครบและเบา) แต่ถ้าต้องการความทันสมัยกว่านี้อีกขั้น พิจารณา **Phosphor Icons** หรือ **Tabler Icons** (เส้นบางสม่ำเสมอ ดูเป็นมืออาชีพกว่าชุดไอคอน default หลายชุด)
- Chart.js: ตั้งค่า `options.plugins.legend`, สี dataset, และ grid line ให้ตรงกับ palette ข้อ 21.1 แทนสี default ของไลบรารี

## 22. เสริมมุมมองนักยุทธศาสตร์งานสาธารณสุข (ป้องกันโรค/ส่งเสริมสุขภาพ)

เพิ่ม 5 แนวคิดจากหลักการวางแผนสาธารณสุขมาตรฐาน เพื่อให้ระบบไม่ใช่แค่ "เครื่องมือบันทึกข้อมูล" แต่ช่วย**วางแผนและจัดลำดับความสำคัญเชิงยุทธศาสตร์**ได้ด้วย

### 22.1 ห่วงโซ่ผลลัพธ์ (Results Chain: Output → Outcome → Impact)
หลักการวางแผนสาธารณสุขมาตรฐาน (WHO Results Chain) แยกตัวชี้วัดเป็น 3 ระดับ เพื่อไม่ให้สับสนระหว่าง "ทำเยอะ" กับ "ได้ผลจริง":
- **Output** — ผลผลิตทันทีจากกิจกรรม (เช่น จำนวนคนที่ผ่านการอบรม, จำนวนครั้งที่คัดกรอง)
- **Outcome** — การเปลี่ยนแปลงพฤติกรรม/สถานะสุขภาพระยะกลาง (เช่น ร้อยละที่ปรับพฤติกรรมหลังอบรม, อัตราการมาตรวจติดตามครบนัด)
- **Impact** — ผลกระทบระยะยาวต่อสุขภาพประชากร (เช่น อัตราป่วย-ตายที่ลดลง)

**การเพิ่มในระบบ:** เพิ่มคอลัมน์ **`result_level`** ENUM(`output`,`outcome`,`impact`) ใน 2 จุด:
- `kpi_master.result_level` — จัดประเภท KPI หลักแต่ละตัว (ส่วนใหญ่ KPI ระดับกระทรวงจะเป็น outcome/impact อยู่แล้ว)
- `key_results.result_level` — เพราะ Key Result ที่ผู้ใช้ตั้งเอง (ข้อ 18) มีทั้งแบบวัด output ง่าย ๆ และแบบวัด outcome ที่ยากกว่า ควรให้ผู้ใช้ระบุตอนสร้าง

กิจกรรม (`activities`) ไม่ต้องมีฟิลด์นี้ เพราะกิจกรรมคือ "กระบวนการ (Process)" ตามนิยามอยู่แล้ว ไม่ใช่ตัวชี้วัด

**ประโยชน์ที่ได้:** Dashboard เพิ่มกราฟใหม่ "สัดส่วน KPI/Objective ตามระดับผลลัพธ์" — ถ้าพบว่าองค์กรตั้งแต่ output เกือบทั้งหมด แทบไม่มี outcome/impact เลย เป็นสัญญาณเตือนว่าอาจ "วัดแค่ว่าทำอะไรไปแล้ว" โดยไม่รู้ว่า "ได้ผลจริงหรือไม่"

### 22.2 ระดับการป้องกันโรค (Prevention Level) ต่อกิจกรรม
เพิ่มคอลัมน์ **`activities.prevention_level`** ENUM(`primary`,`secondary`,`tertiary`, nullable ไม่บังคับ):
- **ปฐมภูมิ (primary)** — ป้องกันก่อนเกิดโรคในกลุ่มคนปกติ (เช่น ให้ความรู้ ส่งเสริมพฤติกรรมสุขภาพ, ฉีดวัคซีน)
- **ทุติยภูมิ (secondary)** — คัดกรอง/ตรวจพบและรักษาแต่เนิ่น ๆ ในกลุ่มเสี่ยง (เช่น คัดกรองมะเร็งปากมดลูก, คัดกรองเบาหวาน)
- **ตติยภูมิ (tertiary)** — ฟื้นฟู/ลดภาวะแทรกซ้อนในกลุ่มที่ป่วยแล้ว (เช่น ฟื้นฟูผู้ป่วยโรคหลอดเลือดสมอง)

**ประโยชน์ที่ได้:** Dashboard/รายงานเพิ่มกราฟวงกลม "สัดส่วนงบประมาณ/กิจกรรมตามระดับการป้องกัน" ต่อกลุ่มงาน/แผนงาน — ช่วยผู้บริหารเห็นภาพว่าพื้นที่ทุ่มเทไปทาง "ป้องกันไว้ก่อน" หรือ "ตามแก้ปัญหาที่ปลายเหตุ" มากกว่ากัน ซึ่งเป็นคำถามยุทธศาสตร์สำคัญของงานสาธารณสุขเชิงรุก

### 22.3 ข้อมูลสถานการณ์ปัญหาจริงในพื้นที่ (Evidence-based Justification) — **บังคับต้องมีก่อนเสนอโครงการ**
เพิ่ม 2 คอลัมน์ใน `projects`:
- **`situation_analysis`** (TEXT, NOT NULL) — สรุปสถานการณ์ปัญหาสุขภาพในพื้นที่ที่ใช้เป็นเหตุผลตั้งโครงการ (เช่น "อัตราป่วยเบาหวานรายใหม่ในพื้นที่เพิ่มขึ้น 15% เทียบปีก่อน จากข้อมูล HDC")
- **`situation_data_source`** (VARCHAR, NOT NULL) — ระบุแหล่งข้อมูลที่ใช้อ้างอิง (เช่น "HDC กระทรวงสาธารณสุข", "43 แฟ้ม", "รายงานเฝ้าระวังโรคประจำปี 2568")

**ยืนยันแล้วว่า "เหตุผล/ที่มาของโครงการ" ต้องบังคับกรอกเสมอ** ครอบคลุมทั้ง 3 รายการ: `situation_analysis`, `situation_data_source`, และ**ระดับความสำคัญ/ที่มา (`project_priority_levels` ข้อ 19) ต้องเลือกอย่างน้อย 1 รายการ** — แต่บังคับแบบ**เดียวกับกฎ "อนุมัติโครงการ ต้องมีกิจกรรมก่อน" ในข้อ 17.3** คือ **ไม่บล็อกตอนบันทึกร่างเริ่มต้น** (สถานะ `1st0` ยังไม่ดำเนินการ) แต่ **บล็อกตอนเปลี่ยนสถานะเป็น "เสนอโครงการ" (`2st1`)** — ผู้ใช้จึงเริ่มร่างโครงการได้ทันทีแม้ยังไม่มีข้อมูลครบ แต่กดปุ่ม "เสนอโครงการ" ไม่ได้จนกว่าจะกรอกเหตุผล/ที่มาให้ครบ พร้อมข้อความเตือนภาษาไทยชัดเจน (ตามมาตรฐานข้อ 14.1) เช่น "กรุณากรอกสถานการณ์ปัญหาและเลือกระดับความสำคัญอย่างน้อย 1 รายการ ก่อนเสนอโครงการ"

แสดงเป็น section แยกในฟอร์มโครงการ ชื่อ "เหตุผล/ที่มาของโครงการ (Situation Analysis)" มีเครื่องหมาย `*` กำกับทั้ง 3 ช่อง — ใช้ `attachments` (ข้อ 3) แนบไฟล์รายงานสถิติประกอบเพิ่มเติมได้ (ส่วนนี้ยังไม่บังคับ เพราะไม่ใช่ทุกกรณีจะมีรายงานเป็นไฟล์)

**ข้อควรระวังตอน implement:** เพราะ `situation_analysis`/`situation_data_source` เป็น `NOT NULL` ในระดับ schema แต่ต้อง**อนุญาตให้ยังไม่กรอกตอนบันทึกร่าง** จึงต้องออกแบบให้ค่าเริ่มต้นเป็น string ว่าง `''` ไม่ใช่ `NULL` จริง ๆ (ต่างจากงบประมาณกิจกรรมในข้อ 20 ที่ default เป็น 0) แล้วใช้ **business rule validation ที่ระดับเปลี่ยนสถานะ** (ข้อ 17.3) เป็นตัวบังคับแทน ไม่ใช่ `NOT NULL` ล้วน ๆ ที่ระดับฐานข้อมูล เพราะจะบันทึกร่างไม่ได้เลยถ้าบังคับตั้งแต่ INSERT แรก — **แยกให้ชัดจาก `priority_levels` ซึ่งเป็นตาราง M:N ต่างหาก (นับจำนวนแถวที่ผูกไว้ ≥ 1 เป็นเงื่อนไข ไม่ใช่ NOT NULL ของคอลัมน์)**

**หมายเหตุ:** คะแนน BPR (`project_priority_scores` ข้อ 22.4) **ยังคงเป็นเครื่องมือเสริมที่ไม่บังคับ** เพราะเป็นการให้คะแนนเชิงตัวเลขที่ต้องใช้วิจารณญาณประเมิน 5 เกณฑ์ PEARL ซึ่งต่างจาก "เหตุผล/ที่มา" ที่เป็นข้อความอธิบายตรง ๆ — ไม่ควรบังคับเพราะจะกลายเป็นภาระกรอกข้อมูลเกินจำเป็นสำหรับโครงการเล็ก ๆ

**ประโยชน์ที่ได้:** เมื่อพิมพ์หนังสือราชการขออนุมัติโครงการ (ข้อ 17.5) สามารถดึงข้อความนี้มาใส่ในเอกสารได้ทันที — และช่วยผู้อนุมัติ (สสอ./รพ./สสจ.) ตัดสินใจได้บนพื้นฐานข้อมูลจริง ไม่ใช่แค่เชื่อชื่อโครงการ

### 22.4 คะแนนจัดลำดับความสำคัญเชิงปริมาณ (Priority Scoring — Hanlon's Basic Priority Rating)
แยกจาก `priority_levels` (ข้อ 19 ซึ่งเป็นแค่ป้ายกำกับที่มา/นโยบาย) เพิ่ม**การให้คะแนนเชิงปริมาณ**ตามสูตรมาตรฐานสาธารณสุข **BPR = (A + 2B) × C × D ÷ 3**

**ตารางใหม่ — `project_priority_scores`** (1 แถวต่อ 1 โครงการ ให้คะแนนตอนเสนอโครงการ):
| คอลัมน์ | ความหมาย | ช่วงค่า |
|---|---|---|
| project_id | FK `projects` | |
| problem_size_score (A) | ขนาดของปัญหา (ประชากรที่ได้รับผลกระทบมากแค่ไหน) | 0–10 |
| problem_severity_score (B) | ความรุนแรงของปัญหา (อัตราตาย/ความสูญเสียทางเศรษฐกิจ/ผลกระทบต่อคนรอบข้าง) | 0–20 |
| intervention_effectiveness_score (C) | ประสิทธิผลของวิธีแก้ไขที่เสนอ (มีหลักฐานว่าได้ผลแค่ไหน) | 0.5–1.5 |
| pearl_propriety, pearl_economics, pearl_acceptability, pearl_resources, pearl_legality | เกณฑ์ความเป็นไปได้ 5 ด้าน (P-E-A-R-L) แต่ละอันตอบ ได้/ไม่ได้ | 0 หรือ 1 |
| computed_bpr_score | คำนวณอัตโนมัติ = `(A + 2B) × C × D ÷ 3` โดย D = ผลคูณของ pearl ทั้ง 5 (ถ้ามีข้อใดเป็น 0 คะแนนรวมจะเป็น 0 ทันที — ความหมายคือถ้าทำไม่ได้จริงในทางปฏิบัติ/กฎหมาย ไม่ควรทำเลยไม่ว่าปัญหาจะใหญ่แค่ไหน) | คำนวณอัตโนมัติ |
| scored_by, scored_at | ผู้ให้คะแนน/เวลา | |

**การใช้งาน:** ให้คะแนนตอนขั้นตอน "เสนอโครงการ" (ก่อนอนุมัติ) — หน้ารายการโครงการเพิ่มตัวเลือก **"เรียงตามคะแนนความสำคัญ (BPR)"** ช่วยให้ สสอ./สสจ. ตัดสินใจว่าเมื่องบประมาณจำกัด ควรอนุมัติโครงการไหนก่อนบนพื้นฐานคะแนนที่มีเหตุผลรองรับ ไม่ใช่ความรู้สึก — ไม่บังคับต้องให้คะแนนทุกโครงการ (เป็นเครื่องมือเสริมการตัดสินใจ ไม่ใช่ gatekeeper บังคับ)

### 22.5 ความต่อเนื่องของโครงการข้ามปีงบประมาณ (Program Continuity)
งานส่งเสริมสุขภาพ/ป้องกันโรคที่ได้ผลจริงต้องทำต่อเนื่องหลายปี ไม่ใช่ทำครั้งเดียวจบ — เพิ่มคอลัมน์ **`projects.parent_project_id`** (FK ชี้กลับไปที่ `projects.id` ของตัวเอง, nullable) เพื่อโยงว่าโครงการปีนี้เป็น "โครงการต่อเนื่อง" จากโครงการปีไหนในอดีต

**พฤติกรรมในฟอร์ม:** ตอนสร้างโครงการใหม่ มีปุ่ม **"สร้างต่อจากโครงการปีก่อน"** (แนวคิดเดียวกับ "เริ่มรอบใหม่จากอันนี้" ของ Objective ในข้อ 18.3) — เลือกโครงการปีก่อนที่จะต่อยอด ระบบ copy โครงสร้าง (วัตถุประสงค์, KPI ที่ผูกไว้, ประเภทโครงการ) มาเป็นฉบับร่าง ตั้ง `parent_project_id` ชี้กลับไปยังโครงการต้นทาง แล้วให้ผู้ใช้ปรับปีงบประมาณ/รายละเอียด/งบประมาณใหม่ตามจริง

**ประโยชน์ที่ได้:**
- หน้ารายละเอียดโครงการแสดง breadcrumb/timeline "สายโครงการนี้ทำต่อเนื่องมากี่ปีแล้ว" ไล่ตาม `parent_project_id` ย้อนกลับไป
- กราฟแนวโน้มข้ามปี (งบประมาณ, ผลลัพธ์ KPI) ของ "โครงการเดียวกัน" ที่ทำต่อเนื่อง แทนที่จะเห็นเป็นโครงการแยกกันคนละก้อนทุกปีอย่างที่เป็นอยู่ตอนนี้ — สำคัญมากเพราะผลลัพธ์ด้านสุขภาพมักต้องใช้เวลาสะสมหลายปีถึงจะเห็นการเปลี่ยนแปลงชัดเจน (โดยเฉพาะ Impact indicator ตามข้อ 22.1)
- ใช้ประกอบการให้คะแนน `intervention_effectiveness_score` (ข้อ 22.4) ได้ด้วย — โครงการที่ทำต่อเนื่องมาหลายปีและเห็นผลจริง ควรได้คะแนนประสิทธิผลสูงกว่าโครงการใหม่ที่ยังไม่มีหลักฐาน

## 23. หน่วยบริการ (Health Facilities) — Master Data ใหม่ เพื่อระบุพื้นที่เป้าหมายอย่างเป็นระบบ

จากการวิเคราะห์ไฟล์แผนจริงหลายฉบับ พบว่ามีการอ้างอิง "รพ.สต./ศสช. รายแห่ง" อยู่บ่อยมาก (เช่น "รพ.สต. 12 แห่ง", "Health Station 5 แห่งพื้นที่เข้มข้น") แต่ระบบเดิมมีเพียง `departments` ซึ่งเป็น**กลุ่มงานภายใน สสอ.** (เช่น กลุ่มงานส่งเสริมสุขภาพ) ไม่ใช่หน่วยบริการจริงที่ให้บริการประชาชนในพื้นที่ ทำให้ไม่มีทางระบุ/นับ/รายงานผลแยกตามหน่วยบริการได้อย่างเป็นระบบ — เพิ่มตารางใหม่และจุดผูกที่เกี่ยวข้องดังนี้

### 23.1 ตารางใหม่ — `health_facilities`
| คอลัมน์ | ความหมาย |
|---|---|
| id | รหัสอ้างอิง |
| code | รหัสหน่วยบริการ (ถ้ามี เช่น รหัสสถานพยาบาล 5 หลักตามมาตรฐานกระทรวง) |
| name | ชื่อหน่วยบริการ เช่น "รพ.สต.บ้านสิงห์" |
| facility_type | ENUM(`รพ.สต.`, `ศสช.`, `รพ.`, `สสอ.`, `อื่นๆ`) |
| network_id | FK `health_networks` (ข้อ 3) — สังกัดเครือข่ายไหน |
| tambon | ชื่อตำบลที่ตั้ง (ใช้กรอง/รายงานเชิงพื้นที่) |
| is_active | ปิดใช้งานได้โดยไม่ต้องลบ (กรณีหน่วยบริการยุบ/ควบรวม) |

เพิ่มเป็น**ตารางข้อมูลหลักตัวที่ 12** ที่ admin จัดการได้ในเมนู "ตั้งค่าระบบ / จัดการข้อมูลหลัก" (ข้อ 10) — รวมทั้งหมดเป็น **12 ตาราง**

### 23.2 ผูกกับโครงการ/กิจกรรม (M:N — ไม่บังคับ ยังใช้ text อิสระคู่กันได้)
- **`project_facilities`** (project_id, facility_id) — หน่วยบริการที่เข้าร่วม/เป็นเป้าหมายของโครงการทั้งหมด
- **`activity_facilities`** (activity_id, facility_id) — หน่วยบริการที่เกี่ยวข้องเฉพาะกิจกรรมนั้น (ละเอียดกว่าระดับโครงการ เช่น กิจกรรม "Smart Health Station" อาจเจาะจงแค่ 5 แห่ง ขณะที่โครงการโดยรวมเกี่ยวข้องกับทั้ง 12 แห่ง)

ฟอร์มโครงการ/กิจกรรม (ข้อ 4) เพิ่มช่อง **"หน่วยบริการที่เกี่ยวข้อง"** เป็น multi-select เลือกจาก `health_facilities` — ไม่บังคับกรอก ยังคงเก็บ `target_area` เป็น free text คู่ขนานไว้สำหรับกรณีอธิบายเชิงพื้นที่ที่ไม่ผูกกับหน่วยบริการเฉพาะเจาะจง (เช่น "ทั้งอำเภอ")

### 23.3 ผลกระทบต่อสิทธิ์การมองเห็นข้อมูล (ทบทวนข้อ 6)
พบว่า `level = 'หน่วยบริการ'` (ผู้ใช้ระดับ รพ.สต./ศสช.) เดิมอิงสิทธิ์ผ่าน `users.office_id` (FK `departments`) ซึ่งเป็นกลุ่มงานภายใน สสอ. **ไม่ใช่หน่วยบริการจริงที่ผู้ใช้กลุ่มนี้สังกัด** — แก้ไขดังนี้:
- เพิ่ม **`users.facility_id`** (FK `health_facilities`, nullable) สำหรับผู้ใช้ระดับ `หน่วยบริการ` โดยเฉพาะ (ผู้ใช้ระดับ สสอ./รพ./สสจ. ยังคงใช้ `office_id` ตามเดิม)
- เพิ่ม **`projects.owner_facility_id`** (FK `health_facilities`, nullable) เป็น**ทางเลือกคู่กับ** `owner_office_id` เดิม — โครงการที่เสนอโดย รพ.สต. ใช้ช่องนี้แทน (มีค่าใดค่าหนึ่งระหว่าง office/facility ไม่ใช่ทั้งคู่)
- กฎกรองข้อมูล (ข้อ 6) เพิ่มเงื่อนไข: ถ้า `level = 'หน่วยบริการ'` ให้กรองด้วย `owner_facility_id = users.facility_id` แทนที่จะใช้ `owner_office_id` เหมือนระดับอื่น

### 23.4 ประโยชน์ที่ได้เพิ่ม
- Dashboard/รายงานนับ **"กี่หน่วยบริการเข้าร่วมโครงการนี้"** ได้จริงจากข้อมูลเชิงโครงสร้าง ไม่ต้องแกะจากข้อความอิสระ
- กรองรายการโครงการ/กิจกรรมตามหน่วยบริการหรือตำบลได้ (เพิ่มเป็นตัวกรองใหม่ในหน้ารายการโครงการ ข้อ 4)
- ผู้ใช้ระดับ รพ.สต. เห็นเฉพาะโครงการของหน่วยตนเองถูกต้องแม่นยำขึ้น ตรงกับที่ออกแบบไว้ในข้อ 6 ตั้งแต่ต้น แต่ตอนนี้อ้างอิงหน่วยบริการจริงแทนกลุ่มงานภายใน สสอ.
- เป็นข้อมูลตั้งต้นที่ใช้อ้างอิงต่อได้ในอนาคต เช่น เชื่อมกับรหัสสถานพยาบาลมาตรฐานกระทรวงเพื่อดึงข้อมูลจาก HDC มาเทียบอัตโนมัติ (ไม่บังคับตอนนี้ แต่ออกแบบโครงสร้างเผื่อไว้แล้วผ่านคอลัมน์ `code`)

โครงสร้างในเอกสารนี้พร้อมสำหรับนำไปพัฒนาต่อใน VS Code ได้ทันที — ยังไม่มีการเขียนโค้ดใด ๆ ในขั้นตอนนี้ตามที่ระบุไว้
